# СВХ «Кара-Суу» — ПОДКЛЮЧЁН 20.08.2026; с 07.09.2026 АИС через центр, UniServer ВЫКЛЮЧЕН, агент напрямую на COM4

**07.09.2026 (день).** Программисты АИС «СВХ» перевели интеграцию по объекту 0011 на центр (`/api/v2`, тот же токен): первая реальная операция — тарирование TAR000013199 (06KG832ATG/06KG009PK, 15 350 кг) в 12:08:11, запись #677, снимки в центре; до этого в 11:20–11:23 программисты проверили новый код `ERR_TARE_TOO_HEAVY` на гружёной машине 32 130 кг (ТС «TEST») — отказ отработал. Игорь в тот же день **выключил UniServer насовсем**: модули `SERIALPORT_WI1`, `WEIGHTINDICATOR1`, `SERIALTCPIP1` OFF, затем по AnyDesk служба `UniServerAuto` остановлена и отключена (`sc stop` + `sc config … start= disabled`). **Агент читает индикатор напрямую с COM4** (плата **WCH PCI Express-SERIAL**, на ней COM3 и COM4; COM1 — встроенный) — порт переключён из панели центра («Настройки» весов → COM-порт = COM4, 9600), агент перезапущен, экран показывает `COM4 · 9600 · 8-N-1`. Ретрансляция SERIALTCPIP1 → socket://127.0.0.1:4001 больше не используется. Уроки переключения (все четыре — задачи в PROGRESS «Вопросы к Игорю»): (1) пока UniServer держал COM4, агент откатывался на socket:// через 12 с, а панель после «Сохранить» говорила «отправлены агенту» — откат виден только в логе центра; (2) тумблеры модулей UniServer порт НЕ освобождали, пока не выключен SERIALTCPIP1 (ссылается на SerialPort_WI1 как источник); (3) строка порта на экране агента собирается при старте и после смены порта на лету не обновляется до перезапуска; (4) каждое «Сохранить» настроек весов и обрыв потока > 5 с сбрасывают наблюдение платформы — стоявшая на весах машина 06KG832ATG шесть раз получила `ERR_VEHICLE_TIMEOUT` (11:48–11:55), пока не пересъехала. Настройки весов при стоящей на платформе машине НЕ сохранять (до агента 0.4.30). Все четыре урока закрыты вечером 07.09.2026 (decisions 07.09 вечер): центр пишет аудит `scale_settings` и превращает отчёт агента в события «Настройки»/«Порт» и аудит, лог центра виден в контейнере; агент 0.4.30 показывает живой порт и не сбрасывает фиксацию при сохранении с теми же порогами наблюдения.

Источник — выгрузка настроек UniServer `Config-20260820.json` (1020 параметров, 35 плагинов; лежит в `../../docs/справка/выгрузка-карасуу/` ВНЕ git — содержит пароль камер) + CSV журнала за 20.08 + скриншоты панели UniServer. `ais_object` объекта в АИС — **`0011`**, весы одни (`AutoScale1`, «Весы 1») → `scale_no` 1, legacy-ключ v1 — `192.168.156.183:8087/autoscale 1`. Маршрут сеть объекта → ВМ центра открыт системщиком 20.08.2026.

## Весы и индикатор — протокол «VESAR», ПЕРВЫЙ НЕ-CAS ОБЪЕКТ

Весы — **статические SCS-80, платформа 18×3 м, НПВ 80 т (Китай)** (сообщил Игорь 20.08.2026) — та же модель, что на Кызыл-Кые, но индикатор другой (на К-К — CAS CI-201A с протоколом cas22; здесь — «VESAR», модель по шильдику ещё не снята).

**Драйвер cas22 НЕ подходит — нужен новый драйвер агента** (первый случай на четырёх объектах; этап 2 «драйверы других индикаторов» начинается здесь).

Разбор протокола: настройки WeightIndicator1 (пресет «VESAR») + **живой дамп 20.08.2026 18:47** (`vesar-dump-20260820-empty-0kg.bin` в выгрузке: 1501 пакет за 120 с, все ровно 12 байт, весы пусты; снят через SERIALTCPIP1 Mode=Client → приёмник на рабочем месте — входящие на весовой ПК режет брандмауэр Windows):

- **Формат вскрыт полностью** (второй дамп 20.08.2026 19:06, оператор ~80 кг на весах: `vesar-dump-20260820-person-80kg.bin`, 4501 пакет, 18 уникальных — заход/стояние/сход):
  - Пакет **12 байт**, ~**12,5 пакетов/с**: `[0]`=`0x02` STX · `[1]`=знак `'+'`/`'-'` · `[2..8]`=**масса, 7 цифр ASCII с лидирующими нулями** · `[9..10]`=**контрольная сумма: XOR байтов [1..8] двумя ASCII-hex цифрами** (подтверждена на всех 18 уникальных пакетах: `+0000000`→`1B`, `+0000850`→`16`) · `[11]`=`0x03` ETX.
  - **Флага стабильности в пакете нет** (в переходах менялась только масса и КС) — вычислять программно, как UniServer (успокоение 1 с / 3 дискреты).
  - **Младший разряд — десятые кг**: стоящий оператор давал устойчиво `0000850` = 85,0 кг при «80» на табло (дискретность табло 10 кг), переходные значения шли с шагом 50 = 5,0 кг. Квантование к дискрете индикатора (10 кг, вероятно вниз — как ведёт себя табло и как UniServer огрублял на Д-А) — уточнить на сверке с UniServer по реальным машинам.
  - UniServer сам читает массу из адресов [1..7] (`'+000085'` = 85 кг — теряет разряд десятых) и КС игнорирует; наш драйвер будет читать [2..8] полностью и проверять XOR.
- Поток непрерывный: опроса нет (Request 0), команд инициализации/обнуления нет — как у cas22.
- Порт **COM4**, 9600 8-N-1, управление потоком None, **DTR/RTS не поднимать** (в UniServer обе линии выключены); переоткрытие при ошибке/без данных включено, тайм-аут 5 с. Переходник COM4 (PCI-карта или USB) — уточнить в диспетчере устройств.
- **НмПВ 50 дискрет = 500 кг** (К-К/Аламедин — 200 кг): учесть при настройке порогов цикла в центре (наш дефолт zero 200 / vehicle 500 — порог «весы пусты» обсудить при установке).

## Весовой ПК и UniServer

- UniServer **2.1.1.141** (win32_amd64, сборка 24.07.2025), `http://192.168.156.183:8087` → IP весового ПК **192.168.156.183**. Имя ПК `DESKTOP-CVSJC1N`, ОЗУ 32 ГБ («Windows 8 64bit 9200» в «О программе» — обычный артефакт манифеста; реальную версию смотреть `winver`). Диск для агента (C:/D:) — уточнить.
- (до 07.09.2026; UniServer выключен, ниже — история) **SERIALTCPIP1 уже включён** (скриншот: «Выполняется», получено 0 Б/с), но в выгрузке 20.08 модуля нет — параметры не видны. Проверить/настроить по рецепту Аламедина: `Mode=Server, Host=127.0.0.1, Port=4001, SourceRepeaterSerialPort=SerialPort_WI1`; com0com не пробовать вовсе (урок 19.08). «0 Б/с» при живом индикаторе — признак ненастроенного Source.
- `AutoScale1.glob_AutoFixingMode = 1` — журнал засоряют «Автофиксация после съезда» (видно и в CSV); показать Игорю, где выключить (как на Аламедине).
- Режим НЕТТО **Double** (раздельные записи БРУТТО/ТАРА — видно в CSV), автовзвешивание без оператора выключено («Вручную оператором»), автосоздание документа Never, автопечать выключена. Период тары UniServer 30 дней (для нас неважно — реестр центра, правило №4).
- Журнал SQLite, нумерация `%.6d` в пределах года, последний документ **11383** от 20.08.2026 18:08 → **в среднем ~49 документов/день — самый нагруженный объект из четырёх** (в CSV за 20.08 — 20 операций к вечеру). Номера ТС и киргизские (075AIJ06KG), и узбекские (P17012, AH6813) — граница.
- Обвязка не подключена: порты `SerialPort_LED1/2`, `SerialPort_UDVV1`, `SerialPort_ScanRFID1/2` пустые (табло/шлагбаумов/RFID нет). Recognize1/2 настроены — нам не нужно (номера придут от АИС).
- Свидетельство о поверке в UniServer не заполнено (`Report_ScaleDoc` пуст) — номер/даты взять у Игоря, завести в «Настройках» весов центра.

## Камеры — Hikvision, ISAPI работает (проверено 20.08.2026)

Две IP-камеры, слоты Camera3/4 пустые → фото на операции — 2 шт. По вебу — **Hikvision** (новая прошивка «Welcome»); XM-подобный RTSP-URL в UniServer (`/channel=1&stream=0.sdp`) — наследие, ISAPI отвечает штатно. Проверено с рабочего места (digest, учётка из выгрузки):

| Камера | Адрес | Проверка 20.08.2026 |
|---|---|---|
| камера 1 | `192.168.156.201` | `/ISAPI/Streaming/channels/101/picture` → JPEG **3200×1800** (~0,7 МБ); `102` → 640×360 (~24 КБ) |
| камера 2 | `192.168.156.202` | то же: 101 → 3200×1800 (~0,8 МБ); 102 → 640×360 (~25 КБ) |

- **Путь агента — HTTP-снапшот, ffmpeg не нужен**; превью-канал 102 заведён с первого дня.
- Обе камеры смотрят на платформу с противоположных торцов (навес, рядом элеватор). **Роли ПОДТВЕРЖДЕНЫ по живому заезду 20.08.2026 и поменяны местами: front = .202, rear = .201** — догадка оказалась обратной ТРЕТИЙ объект подряд; правило прежнее: роли только по живым снимкам.
- Учётные данные камер — только в выгрузке/конфиге агента (правило №7). Часы камер сбиты (OSD «01-01-1970») — NTP в хвосты, как везде.
- UniServer декодирует поток в 1280×720 — на нас не влияет.

## Открытое

**АГЕНТ УСТАНОВЛЕН И ОНЛАЙН 20.08.2026 вечером** (ставил Игорь по `УСТАНОВКА-КАРАСУУ.md`): служба «ves-agent» в `D:\vesy-agent`, порт `socket://127.0.0.1:4001` (SERIALTCPIP1 возвращён в Mode=Server/Host=127.0.0.1), вес идёт, камеры ОК; агент 0.4.23 при установке → 0.4.24 раскаткой канала pilot в тот же вечер (самопроверка пройдена).

- [x] Выгрузка UniServer разобрана; маршрут до центра открыт (20.08.2026); код АИС 0011 известен.
- [x] Живой дамп при пустых весах снят (20.08.2026, структура пакета подтверждена); брандмауэр ПК режет входящие — дампы снимались через SERIALTCPIP1 Mode=Client на рабочее место.
- [x] Дамп под нагрузкой снят (20.08.2026 вечером, оператор на весах — идея Игоря, машина не понадобилась): формат и контрольная сумма подтверждены, протокол готов к реализации драйвера.
- [x] Драйвер `vesar` реализован (агент 0.4.23, 20.08.2026): `scale.driver = "vesar"` в конфиге, эмулятор и тесты есть, спека `docs/protocols/vesar.md`.
- [ ] Квантование к дискрете 10 кг (усечение вниз, как табло) — сверка с UniServer больше невозможна (выключен); при необходимости сверять с табло по фото.
- [x] Модель весов: SCS-80 18×3 м, 80 т, Китай, статические (Игорь, 20.08.2026).
- [ ] Шильдик индикатора (что такое «VESAR» физически) — фото от весовщика, для реестра; драйверу не блокер.
- [x] Камеры: Hikvision, ISAPI-снапшоты 101/102 работают (проверено 20.08.2026) — ffmpeg не нужен, превью-канал есть.
- [x] В центре заведено (20.08.2026, скрипт через refs_admin, репетиция на dev): объект `kara-suu` (id=5) «СВХ «Кара-Суу»», весы id=4 «Весы SCS-80» (driver vesar, legacy 192.168.156.183:8087/1, АИС 0011/№1), пороги 200/500 + 10/90/5/30/5 (порт остаётся локальным), обе камеры (снапшот 101 + превью 102; роли front=.201/rear=.202 — ДОГАДКА, подтвердить по заезду), агент `karasuu-1` канал pilot; токен — на ВМ `~/agent-token-karasuu.txt` и в конфиге мастер-папки. Роли камер после установки ПОДТВЕРЖДЕНЫ по заезду и поменяны: **front=.202, rear=.201**; подпись индикатора «VESAR (весы SCS-80, 80 т)» задана в «Настройках» весов (агент 0.4.24 применяет из центра).
- [x] Мастер-папка `../../../ves-agent-karasuu/` (вне git, СЕКРЕТЫ) + `ves-agent-karasuu.zip`: app 0.4.23 (driver vesar), config.toml (socket://127.0.0.1:4001, камеры с превью, токен; пути на D: — если только C:, поправить), УСТАНОВКА-КАРАСУУ.md (шаг «вернуть SERIALTCPIP1 в Server/127.0.0.1», UniServer перезапускать только службой), nssm/install-service.bat/update.bat.
- [x] Диск — D: (агент встал в `D:\vesy-agent` по инструкции без правок путей).
- [x] Переходник COM4 — плата **WCH PCI Express-SERIAL** (COM3 + COM4), COM1 встроенный (диспетчер устройств, 07.09.2026). Антивируса на ПК НЕТ (Игорь, 07.09.2026).
- [x] АИС переведена на центр (07.09.2026), UniServer выключен насовсем, агент на COM4 из панели центра (07.09.2026); в `D:\vesy-agent\config.toml` порт поправлен на `COM4` и агент перезапущен (Игорь по AnyDesk, 07.09.2026 ~12:20); служба `UniServerAuto` — `START_TYPE 4 DISABLED`, двоичный файл `D:\VesySoft\UniServerAUTO\BIN20\BIN64\EventServer.exe`; занятость COM4 агентом подтверждена из PowerShell: `New-Object System.IO.Ports.SerialPort COM4… .Open()` → UnauthorizedAccessException «Доступ к порту COM4 закрыт».
- [ ] Свидетельство о поверке; учётка весовщика (заведёт Игорь).
- [ ] NTP камер — OSD на снимках по-прежнему «01-01-1970» (07.09.2026): выставить время в веб-интерфейсе Hikvision. `glob_AutoFixingMode` больше не важен — UniServer выключен.
