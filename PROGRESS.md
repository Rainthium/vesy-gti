# PROGRESS.md — ход разработки

> Правила ведения: Claude Code берёт первую незакрытую задачу текущего этапа, после выполнения ставит `[x]` и дату, новые обнаруженные задачи дописывает в нужный этап. Вопросы, требующие решения человека, — в раздел «Вопросы к Игорю». Статус этапа: 🔵 не начат · 🟡 в работе · ✅ завершён.

**Текущий этап: 1 (MVP, пилот) 🟡 + начат этап 2 (мониторинг сделан) + НАЧАТ ЭТАП 4 (18.08.2026, по решению Игоря — раньше остатка этапа 2: экран «Отчёты» сделан целиком, см. раздел этапа 4). ЦЕНТР на ВМ 192.168.140.70 (панель https://vesy.gti.kg/panel/, выкачена 13.08.2026 с весовой карточкой и фиксами сессий; МОНИТОРИНГ ВЫКАЧЕН 13.08.2026 ВЕЧЕРОМ: миграция d4e5f6a7b8c9 на боевой БД, Telegram ПОДКЛЮЧЁН — бот @vesy_gti_alerts_bot, с 14.08.2026 шлёт в КАНАЛ «Алерты весов ОАО "ГТИ"» (chat_id -1004475491902 в deploy/.env на ВМ; исходная группа мигрировала в супергруппу и сменила id — переведено на канал, старая группа удалена), боевой дашборд показывает счётчики и плашку «всё в порядке»). ПЯТЬ ОБЪЕКТОВ В ПИЛОТЕ (с 19.08.2026 — СВХ «Аламедин», TCP-ретрансляция UniServer; с 20.08.2026 — СВХ «Кара-Суу», ПЕРВЫЙ не-CAS индикатор «VESAR», драйвер vesar; с 26.08.2026 — СВХ «Алтын логистик», Keli D2008 на том же драйвере vesar), АГЕНТЫ v0.4.24 (20.08.2026 — четыре релиза за день: превью preview_url, «Дата и время» в журнале, драйвер vesar, подпись индикатора из центра; Д-А вечером 20.08 офлайн по вине ПК объекта — утром 21.08 вернулся и ДОГНАЛ 0.4.24 stable-раскаткой сам, без команд — механизм каналов отработал как задуман) (раньше: ОБА АГЕНТА v0.4.16 (14.08 — четыре релиза за день кнопками 0.4.13→0.4.16, все тихо; быстрая распаковка подтверждена логом Д-А при включённом 360): СВХ «Кызыл-Кыя» (Hikvision, снапшоты; камеры с 14.08 управляются центром — роли исправлены свапом, «ПЕРЕД» был перепутан с «ЗАД» с установки) и ПЗТК «Джалал-Абад» (агент на D:\vesy-agent, COM14↔COM15, RTSP-камеры через постоянные потоки, антивирус 360 приручён белым списком файла exe). ИДЁТ СВЕРКА веса с UniServer (критерий: К-К — строгое совпадение, Д-А — Δ∈{0,+10} кг, см. «Вопросы к Игорю») — до 100% сверки переключение АИС не делаем. Весовая карточка закрыта целиком 13.08.2026 (печать подтверждена на бумаге, свидетельства заполнены, оператор в контракте v1). МОНИТОРИНГ (этап 2, одобрен Игорем): детекторы в центре (офлайн/индикатор/камеры/очереди/диск, пороги-антидребезг), журнал «События» в панели, счётчики и активные алерты на дашборде (закрыт последний пункт бэклога этапа 1), Telegram-уведомления РАБОТАЮТ (закрыто вечером 13.08). Хвосты ревью 13.08 закрыты: журнал агента по POST, env-переключатель Secure-cookie, окно busy→stop снова секундное (0.4.13). ПОЖЕЛАНИЯ ИГОРЯ 14.08 СДЕЛАНЫ И В БОЮ (см. одноимённый раздел): история тарирований на экране «Тарирования» (Действует/Истекло/Заменено), учётки агентов в центре (operators_report, блок «Учётки на агентах» — сразу нашлись локальные dzhaltest и admin), вопрос смены IP разобран без разработки. Второй виток 14.08: кнопка «Заблокировать» местных учёток (перехват логина двойником) + фильтры экрана на блоке агентов — обе местные учётки (dzhaltest, admin) перехвачены Игорем в тот же день. Третий виток 14.08: роли камер К-К исправлены из панели, попутный баг превью (не подхватывало камеры центра до рестарта) закрыт агентом 0.4.15 — превью подтверждено Игорем. Четвёртый виток 14.08 (вечер): показ устаревшего тарирования — карта без строки «Полная масса», примечание с датой и массой, подсказки оператору, хинт «Тарирований» по формулировке Игоря; расчёт и контракт не тронуты; агент 0.4.16 установлен кнопками, оба объекта в бою. Вечером же Telegram-алерты пережили миграцию группы в супергруппу (первые боевые события доехали задним числом) и переведены в канал. 1433 теста. Дальше по этапу 2: ИНТЕГРАЦИЯ С АИС v2 + RabbitMQ — КОНТРАКТ СОГЛАСОВАН 17.08.2026 (версия 1.0), РЕАЛИЗАЦИЯ ИДЁТ: витки (а) API v2 и (б) outbox+RabbitMQ сделаны 17.08 (команда с идемпотентностью по ais_ref, документ операции, GET, обратный вызов, привязка объектов АИС; outbox офлайн-операций, публикатор aio-pika, детектор «События АИС», кнопка переотправки, RabbitMQ в compose; 1496 тестов), витки (в)–(д) тоже: фильтр «Офлайн без номера АИС», ETag фото, имитатор `tools/ais_client_v2` и консьюмер `tools/ais_consumer`, шаг v2 в e2e; ВЫКАЧЕНО НА ВМ 17.08 ночью — RabbitMQ поднят (очередь `ais-svh.weighings`, учётка ais-svh на чтение, пароль в ~/rabbit-ais-svh.txt на ВМ), весы привязаны 0014/0013, /api/v2 проверен с мака (200), брокер доступен под ais-svh; ждём от Игоря передачи доступов АИС и открытия сети системщиком; АГЕНТ 0.4.17 в бою на обоих объектах (18.08; Д-А — со второй попытки: 360 резал tar/schtasks, открытый Проводник срывал move); ОБА АГЕНТА НА 0.4.18 (18.08, кнопками): сторожок обновления докладывает центру событием, подмена папки с повторами, сессии панели/оператора выбиваются при смене пароля — центр выкачен; /api/v2 в nginx открыт и для рабочих мест разработчиков АИС (192.168.15.10/.2/.4, 18.08); 1528 тестов. АВТОВЫКАТ ПО КАНАЛАМ + САМОПРОВЕРКА/АВТООТКАТ + ЭКРАН «РЕЛИЗЫ» СДЕЛАНЫ, ВЫКАЧЕНЫ И ПРОВЕРЕНЫ В БОЮ 18.08 (агент 0.4.19, 1603 теста): ПЕРВЫЙ АВТОВЫКАТ ПРОШЁЛ — К-К через pilot, Д-А через stable, ОБА АГЕНТА НА 0.4.19 (каналы: К-К pilot, Д-А stable; релиз 0.4.19 = stable). Обновления агентов теперь — экраном «Релизы» («В pilot» → «Перевести в stable»), не кнопками карточек. ЭТАП 4 «ОТЧЁТЫ» СДЕЛАН И ВЫКАЧЕН 18.08.2026 (вечер; коммиты b7f89d0, efe5ba4, 0142898, миграция d0e1f2a3b4c5 на бою): /panel/reports — итоги, массы (нетто = Σбрутто − Σтары по взвешиваниям с тарой; без тары отдельно; причины и сцепки к перетарированию), по объектам с графиками, динамика по дням/неделям/месяцам (таблица под графиками свёрнута), ручные операции, надёжность, Δ к прошлому периоду, печать, CSV/.xlsx (openpyxl); на дашборде — полоска объёмов «Сегодня / 7 дней / Этот месяц» с Δ и ссылками в отчёт; 1655 тестов. Этап 4 в остатке — только по запросу (интеграции помимо АИС). ДАЛЬШЕ (по «Продолжай»): подпись сборок (сертификат за Игорем), драйверы остальных индикаторов (vesar сделан 20.08; впереди динамические XK3190-DM/Уралвес — этап 3), тираж (Кант — два агента на одном ПК), расширения контракта по желанию АИС. ПЯТЫЙ ОБЪЕКТ ПОДКЛЮЧЁН ЗА ОДИН ВЕЧЕР 26.08.2026: СВХ «Алтын логистик» (АИС 0018, поверка №217) — весы «Пегас» 22-80-А-10 (60 т), индикатор Keli D2008, живой дамп показал ПРОТОКОЛ «VESAR» БАЙТ В БАЙТ (новый драйвер НЕ понадобился; поток квантован индикатором к 20 кг → сверка строгая), камеры 4К со снапшотом и ВЕРНЫМИ часами; заведено в центре (объект id=6, весы id=5, агент altyn-1 pilot — боевой прогон скрипта выполнил Игорь), АГЕНТ 0.4.24 УСТАНОВЛЕН (C:\vesy-agent, диска D: нет) И ОНЛАЙН с 20:15, алертов нет; хвосты — роли камер по заезду, строгая сверка, учётка весовщика (см. «Вопросы к Игорю»). 27.08.2026 — заочная оценка ШЕСТОГО кандидата ПЗТК «Беловодское» (SCS-80 80 т, Yaohua XK3190-D2+, БЕЗ UniServer — ProjectBalance 2008 на XP): индикатор менять НЕ нужно, ждём новый ПК (спека закупки в карточке), поверка №6673 истекает 19.09.2026. 02.09.2026 — БОЕВОЙ ЖУРНАЛ ОЧИЩЕН ОТ ТЕСТОВЫХ ОПЕРАЦИЙ по решению Игоря (81 запись, 150 фото, тары; всё до 27.08 включительно; пользователи/справочники целы; бэкап `~/backups` на ВМ; decisions 02.09) — история взвешиваний в центре начинается заново; локальные журналы агентов не чищены; cron-бэкап ОТЛОЖЕН Игорем до полноценного запуска. 02.09.2026 — заочное обследование СЛЕДУЮЩЕГО объекта СВХ «Кокчо-Коз» (АИС 0021, Yaohua XK3190-DS3, БЕЗ UniServer — ВесыСофт; ретрансляция порта com0com+hub4com; ждём сеть от системщика, драйвер xk3190 пишем под него первым; `tools/serial_dump.ps1` для дампа без UniServer). 02.09.2026 — АГЕНТ 0.4.25: срок хранения локальных фото из центра (на лету) + кнопка «Освободить место» + «Диск ПК» на карточке весов — ответ на вопрос Игоря про удалённую чистку диска ПК (записи не удаляем, правило №2; decisions 02.09); РАСКАТАН Игорем на все 5 объектов в тот же вечер. АГЕНТ 0.4.26 (вечер): после уборки журнал агента помнит отказ центра и держит миниатюры из центра на диске (2158 пустых запросов за 4 ч с К-К), вёрстка строки «Диск ПК» на карточке исправлена — РАСКАТАН на 4 объекта, Д-А догонит при выходе на связь (stable). 03.09.2026 — ШЕСТОЙ объект СВХ «Кокчо-Коз» ПОДКЛЮЧЁН за день: драйвер `xk3190` (0.4.27; кадр VESAR в десятых кг, дискрета 20, делитель/дискрета в `[scale]`), штатное ПО объекта выключено — агент прямо на COM1; АИС к объекту не подключена → АГЕНТ 0.4.28: «Ручной режим при связи с центром» — галочка в «Настройках» весов (исключение из правила №3 по решению Игоря, decisions 03.09), центр с миграцией b4c5d6e7f8a9 ВЫКАЧЕН вечером 03.09 (все 6 агентов на связи), релиз 0.4.28 выложен в releases ВМ, мастер-папки всех шести объектов на 0.4.28 — раскатка («В pilot» → stable) и галочка ручного режима на Кокчо-Козе за Игорем. Кокчо-Коз на 0.4.28, ручной режим при связи ВКЛЮЧЁН и ПРОВЕРЕН ручным взвешиванием (для проверки человеком пороги временно 20/60, возвращены на 400/800); 0.4.28 ПЕРЕВЕДЁН В STABLE Игорем вечером 03.09 — ВСЕ ШЕСТЬ агентов на 0.4.28 (Кара-Суу — со второй автоматической попытки: канал объекта сегодня ~25 КБ/с; Д-А догнал сразу). ПРОВЕРЕНО: 5 ручных взвешиваний Кокчо-Коза видны в центре, к ночи 03.09 5 событий опубликованы в очередь `ais-svh.weighings`. 04.09.2026: АИС «СВХ» ПОДКЛЮЧИЛАСЬ к Кокчо-Козу по REST (команды v2 с 10:46 по Бишкеку), Игорь снял галочку ручного режима в 11:14 — очередь `ais-svh.weighings` (vhost `vesy`) никто не читает (120 сообщений, 0 консьюмеров), но все 120 ручных операций объекта АИС забрала сверкой по REST и привязала свои номера (`ais_ref_link` 04.09 14:50). Вечером 04.09 по решению Игоря сделано: сторно из панели (причина обязательна), лимит массы тарирования 25 т (`ERR_TARE_TOO_HEAVY`) и «тара ≥ брутто не подставляется» — агент 0.4.29, центр выкачен; Игорю — сторнировать 23 записи Кокчо-Коза и раскатать 0.4.29. Докачка архива с места обрыва — ОТЛОЖЕНА решением Игоря. Поверка Аламедина в документации исправлена по базе и фото свидетельства: №4906 до 03.03.2027 (в docs с 19.08 по ошибке стояло №5535). СЛЕДУЮЩАЯ СЕССИЯ: срочного нет; проверить, что 23 тарирования Кокчо-Коза сторнированы и 0.4.29 в stable; хвосты — Jevol, Беловодское, Кокчо-Коз мелочи** · Обновлено: 04.09.2026 (вечер)

---

## Этап 0 — фундамент ✅

- [x] Скелет репозитория: `agent/`, `center/`, `shared/`, `tools/`, `docs/`; pyproject, ruff, mypy, pytest, pre-commit (07.08.2026, менеджер пакетов — uv, Python 3.12.13)
- [x] `shared/`: модели сообщений агент↔центр (pydantic), перечисления статусов и кодов ошибок (architecture §4.1) (07.08.2026, черновик v1 контракта: hello/heartbeat/weigh_request/weigh_result/offline_sync + offline_sync_ack/tare_registry)
- [x] `tools/cas22_emulator.py`: эмулятор индикатора CAS 22 byte — TCP-сервер (pyserial `socket://`) + библиотека для тестов; режимы: пустые весы, заезд, стабилизация, нестабильность, обрыв потока, мусор, отрицательный вес, перегруз (07.08.2026; 110 тестов, сверка с эталонным парсером прототипа)
- [x] CI: GitHub Actions — ruff + mypy + pytest на push (07.08.2026)

## Этап 1 — MVP: агент + центр, пилот Кызыл-Кыя 🔵

### Агент
- [x] Драйвер `agent/drivers/cas22.py` по `docs/protocols/cas22.md` (основа — рабочий прототип cas22_reader.py): чтение потока, ресинхронизация, флаг стабильности, автопереоткрытие порта (ошибка 31/CH340), без DTR/RTS (07.08.2026; + интерфейс ScaleDriver/ScaleState в agent/drivers/base.py)
- [x] Юнит-тесты драйвера на эмуляторе: все режимы, включая обрыв и мусор (07.08.2026; 60 тестов: разбор, ресинхронизация, NO_DATA по таймауту, автопереоткрытие через socket://)
- [x] Конечный автомат взвешивания (architecture §3.3): IDLE→WAIT_ZERO→WAIT_VEHICLE→WAIT_STABLE→CAPTURE→DONE, параметры из конфига, коды ошибок ERR_* (07.08.2026; agent/weighing/cycle.py, детерминирован инъекцией часов, 43 кейса; съёмка камер — через CAPTURE/complete_capture вызывающим кодом)
- [x] Модуль камер: снимок из RTSP (ffmpeg) и HTTP-snapshot, две камеры, таймауты, ERR_CAMERA (вес возвращается с предупреждением) (07.08.2026; agent/cameras/capture.py, параллельная съёмка, маскировка паролей в ошибках, 31 тест; данные камер Кызыл-Кыи — docs/objects/kyzyl-kyia.md)
- [x] Локальная БД SQLite: журнал операций, флаг synced, реплика реестра тарирований (07.08.2026; agent/sync/storage.py: триггеры неизменяемости на уровне БД, тара «не старше 3 календарных месяцев», WAL, 53 теста)
- [x] WebSocket-клиент к центру: hello/heartbeat/weigh_request/weigh_result/offline_sync, реконнект с backoff (07.08.2026; agent/sync/ws_client.py: досылка порциями с ack, подталкивание очереди heartbeat'ом, ERR_INTERNAL при падении обработчика, приём tare_registry; 19 тестов)
- [x] Локальный веб-интерфейс (по макетам `design/operator-*`): главный экран (вес, камеры, журнал, режимы онлайн/офлайн), вход оператора, экран «Оборудование» (07.08.2026; agent/web/: FastAPI+Jinja2+HTMX, живой вес по WS, PBKDF2-пароли операторов, вёрстка по дизайн-системе сверена с превью; демо-стенд tools/dev_operator_ui.py; 38 тестов. Ручной режим /manual/* — следующая задача; в «Оборудование» позже добавить: возраст последнего пакета, heartbeat, аптайм, миниатюры фото в журнале)
- [x] Ручной режим (офлайн): форма взвешивание/тарирование, подстановка тары из реплики, расчёт нетто (07.08.2026; agent/weighing/manual.py — фиксация веса+снимков → превью → «Зафиксировать»/«Отмена», серверная проверка правила №3 на всех /manual/*; вёрстка по operator-manual; 56 тестов)
- [x] Оверлей на фото при съёмке: имя камеры, дата/время, зафиксированный вес (как OSD UniServer; критично — часы камер врут, штамп «1970»); однократно в момент снимка, до расчёта sha256 (решение 07.08.2026) (08.08.2026; agent/cameras/overlay.py: полупрозрачная плашка внизу кадра, бишкекское время, JPEG q85 родного разрешения; шрифт DejaVu Sans Bold (сабсет латиница+кириллица, 32 КБ) в комплекте агента — системным шрифтам весового ПК не доверяем; прожиг в shots.store_shots для обоих режимов; деградация: битый кадр сохраняется без плашки; проверен на реальном кадре Кызыл-Кыи 2560×1440)
- [x] Конфиг агента для Кызыл-Кыи по данным выгрузки: порог пустых весов 200 кг, стабильность 5 с, HTTP-snapshot ISAPI как основной путь камер (docs/uniserver-kyzylkia.md §2а) (09.08.2026; agent/config.py — TOML через stdlib tomllib, pydantic-валидация с запретом неизвестных ключей (опечатки видны при старте); agent/config.example.toml — образец Кызыл-Кыи с плейсхолдерами секретов; agent/main.py — оркестратор: сборка драйвер+авторежим+ручной+WS-клиент+загрузчик фото+веб оператора (порт 8090 — 8087 занят UniServer), правило №3 воплощено здесь (manual_allowed = не connected), CLI add-operator; 13 тестов, включая дымовой старт всего агента и валидность примера)
- [x] Уборка снимков-сирот (файлы фото без записи в журнале) при старте агента — делается вместе с оркестратором/упаковкой. Остальные находки ревью ручного режима (сброс превью, флеш при устаревшем preview_id) отпали после перехода на одношаговую фиксацию (см. decisions 07.08.2026) (09.08.2026; agent/main.py cleanup_orphan_photos: при старте, до запуска служб; снимки записей журнала неприкосновенны)
- [x] Упаковка: PyInstaller exe + конфиг + установка службой (nssm), инструкция установки (10.08.2026; `packaging/agent/`: spec onedir + entry.py + install-service.bat/update.bat/build.bat; workflow «Сборка агента» на windows-runner — по тегу `agent-v*` архив `ves-agent-<версия>-win64.zip` прикрепляется к релизу GitHub, внутри app/ + config.example.toml + батники + УСТАНОВКА.md; инструкция — `docs/install-agent-windows.md`; frozen-сборка проверена запуском: веб/шаблоны/статика работают; первый релиз собран и проверен: https://github.com/Rainthium/vesy-gti/releases/tag/agent-v0.1.0)

### Центр
- [x] БД PostgreSQL: схема по architecture §5 (+ alembic-миграции) (08.08.2026; center/db/: 10 таблиц, триггеры неизменяемости weighings/weighing_photos, частичный уникальный индекс legacy-маршрута NULLS NOT DISTINCT, контрольная сумма записи sha256, TIMESTAMPTZ везде; docker-compose dev-postgres:5443; postgres в CI; 43 теста)
- [x] При развёртывании центра: отозвать привилегию TRUNCATE на weighings/weighing_photos у рабочей роли БД (строчные триггеры неизменяемости TRUNCATE не ловят) — ВЫПОЛНЕНО 10.08.2026 на боевой ВМ 192.168.140.70 сразу после первого запуска (шаг 4 deploy/README.md; при развёртывании новых объектов/переустановке повторять)
- [x] WebSocket-сервер агентов: регистрация по токену, heartbeat → статусы, приём результатов и офлайн-досылки, раздача реестра тарирований (08.08.2026; center/agents_ws/: hub с ожиданием weigh_result по request_id и вытеснением старых соединений, идемпотентный приём записей, upsert тары с защитой от досылки не по порядку, broadcast реестра; сквозная проверка с настоящим агентским CenterClient; 44 теста)
- [x] API v1 (совместимый с АИС): маршрутизация по legacy ip/autoscale, поле operation, ответ с tare/netto, коды ошибок; тайм-аут всей операции; все даты — ISO 8601 с поясом +06:00 (08.08.2026; center/api_v1/ + center/app.py; в запрос добавлены vehicle_number/trailer_number — см. decisions; все исходы HTTP 200 + code; аудит команд без пароля; 39 тестов)
- [x] `tools/ais_client.py`: имитатор АИС «СВХ» — шлёт запросы v1 (weighing/taring) и печатает ответ; реальную АИС на этапе разработки и пилота НЕ трогаем (08.08.2026; stdlib-клиент, коды выхода для скриптов, пароль в консоли маскируется)
- [x] Приём и хранение фото: JPEG как есть, sha256, миниатюры, раздача с сервисными токенами (АИС) + IP-allowlist + аудит скачиваний (08.08.2026; center/photos/: атомарная запись, идемпотентность со сверкой sha, канонические пути формирует центр, миниатюры Pillow при приёме; раздача через nginx — к задаче развёртывания; 36 тестов)
- [x] Фото офлайн-записей — РЕШЕНО 08.08.2026: метаданные в WeighingRecord.photos (оба канала), файлы догружает agent/sync/photo_uploader.py с ретраями (см. decisions)
- [x] Очередь загрузки фото: счётчик попыток и ротация (11.08.2026; attempts/next_attempt_at у снимка, пауза после неудачи растёт 15 с → 30 с → … → 30 мин, выборка ORDER BY attempts, created_at, role — вечно падающее фото уходит в хвост и не задерживает свежие; на пятой неудаче однократное предупреждение в лог; «сдаваться» нельзя — фото доказательство, остаётся в очереди навсегда)
- [x] Веб-панель (по макетам `design/center-*`): дашборд объектов, журнал с фильтрами и карточкой, реестр тарирований, справочники, вход (08.08.2026; center/web/: только чтение, фильтры журнала по моменту взвешивания, фото по сессии панели, CLI tools/center_admin.py для справочников и выпуска токенов, демо-сеятель; вёрстка по center-* сверена с превью; 60 тестов)
- [x] Дашборд объектов: рядом с состоянием агента — состояние весового индикатора (ОК/нет данных + текущий вес) и КАЖДОЙ камеры (запрос Игоря 09.08.2026: все проблемы видны на одном экране) (09.08.2026; агент: CameraHealth — фоновая проверка камер раз в camera_check_interval_s (60 с, конфиг), статусы в hello/heartbeat; центр: хаб хранит последнюю самодиагностику, очищает при разрыве (стухшие статусы не вводят в заблуждение), карточка весов на дашборде показывает всё; 6 тестов)
- [x] Тара по паре голова+прицеп (решение Игоря 09.08.2026): реестр тар с ключом (голова, прицеп), тара подставляется только при полном совпадении сцепки; тарирование без прицепа действует только для машины без прицепа (09.08.2026; миграция центра, реплика агента пересоздаётся автоматически, оба режима, фолбэк v1, подсказка формы, реестр панели; правило №4 обновлено)
- [x] ERR_CAMERA = отказ операции: без снимков обеих камер взвешивать/тарировать нельзя (решение Игоря 09.08.2026; агент авто+ручной не проводят операцию и не создают запись, v1 отдаёт {code, message}; контракт и architecture §4.1 обновлены)
- [x] При развёртывании: обязательные env-секреты (PANEL_SECRET, AIS_PHOTO_TOKEN, V1_PASSWORD, пароль БД) — центр должен отказываться стартовать в проде с dev-дефолтами (08.08.2026; CENTER_ENV=production в center/app.py: отказ старта с перечнем проблем; V1_PASSWORD=admin допустим, но только явно)
- [x] docker-compose: app + postgres + nginx; README по развёртыванию на ВМ Ubuntu (08.08.2026; deploy/: Dockerfile (uv, только код центра+shared), compose с одноразовым migrate-контейнером, nginx-прокси (WS-апгрейд /agents/ws, таймауты под V1_WEIGH_TIMEOUT_S), .env.example, README: docker, секреты, REVOKE TRUNCATE, справочники через center_admin, TLS, бэкапы, обновление; /healthz для healthcheck'ов)
- [x] Сквозной тест: эмулятор весов → агент → центр → запрос «как от АИС» → ответ с весом и фото (08.08.2026; tests/test_e2e_stage1.py — все компоненты настоящие: эмулятор по TCP, Cas22Driver, автоматический цикл, HTTP-камеры, SQLite, WebSocket, uvicorn, PostgreSQL; сценарий: тарирование 8 т → реплика реестра тар на агенте → взвешивание 20 т с tare/netto → догрузка и скачивание фото байт-в-байт; попутно: agent/weighing/auto.py — оркестратор автоматического режима (weigh_request → цикл → камеры → запись, ERR_BUSY при параллельной команде, двухканальная доставка), общий кирпич shots.py, корректная остановка эмулятора при живых клиентах)

### Пилот (руками, вместе с Игорем)
- [x] Развернуть центр на ВМ Ubuntu (10.08.2026; боевая ВМ 192.168.140.70 (Ubuntu 22.04, 4 vCPU/8 ГБ/487 ГБ), docker-compose поднят, миграции применены, TRUNCATE отозван, секреты в deploy/.env на ВМ, панель http://192.168.140.70/panel/ работает; справочник: объект kyzyl-kyia + весы (legacy 192.168.158.20:8087; autoscale=1 — подтверждён Игорем 12.08.2026, номер весов на объекте) + токен агента (~/agent-token-kyzyl-kyia.txt на ВМ); обновление центра: ./deploy/ship.sh vesy@192.168.140.70 — сборка образа на рабочей машине, на ВМ не собираем (порядок Игоря))
- [x] Установить агента на весовой ПК Кызыл-Кыи (10.08.2026; параллельно с UniServer через штатную ретрансляцию SourceRepeaterSerialPort → пара com0com COM10↔COM11; подключён к центру напрямую ws://192.168.140.70/agents/ws; пробное взвешивание прошло — запись OK 27 080 кг с фото в центре; хвосты установки закрыты: служба «ves-agent» через install-service.bat, боевые операторы заведены из центра — пометка обновлена 18.08.2026)
- [ ] N дней параллельной работы, сверка веса с UniServer/табло — критерий: 100% совпадение (АИС продолжает работать через старую цепочку)
- [ ] Переключение АИС «СВХ» на центр — сразу на контракт v2 (согласован 17.08.2026; v1 остаётся заморожен как страховка/имитатор) — ОТДЕЛЬНЫЙ шаг после успешного пилота, по решению Игоря и готовности разработчиков АИС; до этого проверки только имитаторами tools/ais_client.py (v1) и tools/ais_client_v2.py

## Замечания Игоря по итогам пробного взвешивания (10.08.2026, делать в этом порядке)

- [x] Центр: не сохранять и не показывать неуспешные операции (10.08.2026; repo.save_weighing_record отвергает code != OK — отказы не пишутся вовсе; журнал панели фильтрует code == OK, исторические ERR-строки скрыты; ответ АИС строится из живого weigh_result — контракт v1 не задет)
- [x] Списки взвешиваний/тарирований панели: миниатюры фото ПЕРЕД/ЗАД в строке + lightbox (10.08.2026; макрос _thumbs.html, queries.photos_for_weighings, static/lightbox.js — ванильный оверлей с Escape/крестиком/кликом по фону)
- [x] Карточка записи: фото открывается в lightbox-окошке вместо новой вкладки (10.08.2026)
- [x] АВТООБНОВЛЕНИЕ АГЕНТОВ ИЗ ЦЕНТРА (10.08.2026, агент v0.3.0; центр раздаёт релизы из deploy/releases (том :ro, токен агента, строгие имена), кнопка «Обновить до vX» на дашборде → WS update_command {version,url,sha256,size} → агент: подготовка до ожидания весов, sha256+size, защита от zip-slip, self-update.bat с откатом в app_old, отчёт update_status; deploy/README §9. ⚠️ Агент на весовом ПК должен один ПОСЛЕДНИЙ раз обновиться руками до v0.3.0 — старые версии команду не понимают)

## Бэклог панели и агента (после MVP, до/параллельно этапу 2)

- [x] Панель: экраны администрирования ПОЛЬЗОВАТЕЛЕЙ (создание, роли, сброс пароля, отключение) (10.08.2026; экран /panel/users, вкладка видна только admin; center/web/users_admin.py: пароль ≥8, admin/admin запрещён, логин по белому списку [a-zA-Z0-9._-]{1,64} (инъекции в разметку исключены), привязка к объекту; защита от локаута: себя не отключить, последнего активного админа не отключить/не разжаловать; пользователи не удаляются — только отключаются; права на экран сверяются с БД на каждый запрос (разжалованный админ теряет доступ сразу); CLI create-user остаётся для первого админа и аварийного доступа; 47 тестов. Примечание ревью: инвариант «последний админ» не сериализован против одновременных запросов — при гонке восстановление через CLI)
- [x] Панель: экраны редактирования СПРАВОЧНИКОВ (объекты, весы, камеры, агенты, выпуск токенов) (10.08.2026; вкладка «Справочники»: чтение для всех, формы — только admin (серверная проверка по БД). Объекты: создание (код-слаг [a-z0-9-] неизменен после создания — входит в пути фото) и переименование, не удаляются. Весы: создание/правка, legacy-маршрут АИС строго целиком (IP октеты ≤255, порт 1..65535, autoscale 1..999), конфликт маршрута ловится. Агенты: создание с выпуском токена (показ ОДИН раз через flash-сессию, не в URL, Cache-Control: no-store; в БД только хеш), перевыпуск токена (старый гаснет сразу, confirm), канал pilot/stable. Камеры: snapshot/RTSP URL с паролями прямо из панели (решение Игоря — управлять без AnyDesk; в логах URL не светятся); доставка изменений агенту на лету — со страницей настроек весов. CLI center_admin остаётся аварийным. 104 теста. Примечание ревью: CLI умеет legacy-маршрут без порта, панель требует полный — при пересохранении таких весов порт придётся дописать)
- [x] Время взвешивания от центра (10.08.2026; вопрос Игоря. heartbeat_ack с server_time на hello (первым) и каждый heartbeat; agent/clock.py CenterClock — смещение часов, weighed_at и оверлей фото по времени центра; офлайн — последнее смещение из SQLite (переживает рестарт), совсем без связи — локальные часы; агентам <0.4.0 ack не шлётся — гейт версий)
- [x] Панель: страница настроек каждых весов + камеры и COM-порт из центра (10.08.2026; предложение Игоря 09.08 + дополнение 10.08 «настраивать без AnyDesk». Страница «Настройки» весов в Справочниках (admin): 7 параметров цикла (scales.thresholds, валидации порогов/таймаутов), COM-порт+скорость (scales.port_cfg); URL камер — вкладка справочников. Доставка scale_config при hello и push при сохранении; агент применяет на лету: цикл — сброс наблюдателя в WAIT_EMPTY, камеры — все потребители, порт — перезапуск драйвера с ОТКАТОМ при молчании индикатора 12 с (config_status rolled_back); снимок в SQLite переживает рестарт (agent/settings.py). Токен агента каналом не передаётся. ГЕЙТ ПО ВЕРСИИ: снимки с секретами (операторы, настройки) уходят только агентам ≥0.4.0 — старый ws_client логировал бы их кусками (правило №7); версия агента поднята до 0.4.0. Драйвер: пер-запусковый stop-event — клин CH340 не блокирует смену порта. 80 тестов. ⚠️ Канал ws:// нешифрованный — пароли камер идут открытым текстом по внутренней сети (довод за wss, вопрос к системщику). Примечание: локальный timeout_s камер при управлении из центра заменяется дефолтом; очистка порта в панели вернёт локальный порт после рестарта агента)
- [x] Операторы агента через центр (10.08.2026; operators_registry полным снимком, см. decisions): учётки заводятся/блокируются/меняют пароль на вкладке «Пользователи» (роль «оператор» + объект) и реплицируются на агентов хешами — при связи мгновенно, офлайн-вход по локальной копии, блокировка действует и офлайн; CLI add-operator остаётся аварийным доступом. Решение Игоря: боевых операторов Кызыл-Кыи заводим этим механизмом (тестовый тогда сносится), на весовом ПК руками больше не создаём. Порядок при деплое: снимки с секретами защищены гейтом версий (агентам <0.4.0 центр их НЕ шлёт — утечки в лог старого агента нет), поэтому сначала обновить агента до 0.4.0 кнопкой панели, потом заводить операторов — до обновления реплика до него просто не доедет
- [x] Панель: фильтры и поиск на экране «Пользователи» (логин/ФИО/роль/объект/статус, LIKE-знаки экранируются) и общий фильтр по объекту на «Справочниках» (сужает весы/агентов/камеры разом, преселект в форме создания весов) — запросы Игоря 11.08.2026, в тот же день выкачено на ВМ
- [x] Несколько весов на объекте: второй агент на том же весовом ПК (11.08.2026; разбор — decisions. Имя службы nssm больше не константа: install-service.bat принимает его аргументом, проверяет (латиница/цифры/.-_, до 64 символов) и записывает в service.txt; агент читает файл и строит из имени и команды bat, и имя одноразовой задачи планировщика «<служба>-update» — обновления двух агентов не мешают друг другу. Файла нет = историческая установка (боевой Кызыл-Кыя, служба ves-agent) — поведение прежнее; файл ЕСТЬ, но испорчен — обновление отменяется с внятной ошибкой центру (тихий откат к ves-agent остановил бы соседа — находка ревью). Имя cookie интерфейса оператора включает порт (cookie к порту не привязан: два агента на одном ПК выбивали бы друг друга из сессии). update.bat берёт имя из service.txt. docs/install-agent-windows.md — раздел «Второй агент на том же ПК»: обязательно свои [storage] db_path/photos_dir (общий каталог фото = агент при старте сотрёт снимки соседа, правило №2), свой веб-порт, вторая пара com0com и вторая ретрансляция COM в UniServer — работа на объекте)
- [x] Панель: весы в списках взвешиваний и тарирований (11.08.2026; общий макрос _filters.html: селектор «Весы» подчинён выбранному объекту — без объекта весы подписаны его именем, при выбранном объекте показаны только его; колонка «Объект / весы» в журнале и реестре тарирований; реестр тарирований получил фильтры по объекту и весам (queries.tare_list); весы чужого объекта в фильтре сбрасываются, а не дают молча пустой список)
- [x] Справочники: объект рядом с весами в таблицах «Агенты» и «Камеры» + предсказуемый порядок строк (11.08.2026; названия весов на разных объектах совпадают — «Весы SCS-80»; refs_data отдаёт объект в строках камер и агентов и сортирует по имени объекта, затем весов — прежней сортировки только по объекту не хватало, двое весов одного объекта прыгали местами между обновлениями)
- [x] Миниатюры фото в списках взвешиваний с раскрытием по клику — панель центра (10.08.2026) и интерфейс оператора агента (11.08.2026; колонка «Фото» в журнале агента, лайтбокс тот же, что в панели; миниатюра строится один раз и кладётся рядом с кадром, оригинал не трогается)
- [x] Журнал агента: фото с фолбэком на центр (11.08.2026; agent/photos.py: пока локальный файл есть — отдаём его, после ретеншна берём у центра новым GET /agents/photos/{uuid}/{role} по токену агента, который отдаёт снимки ТОЛЬКО своих весов; офлайн и отказ центра — прочерк в строке, не ошибка)
- [x] Ретеншн локальных фото агента (11.08.2026; agent/sync/retention.py: раз в 6 часов убирает файлы снимков, подтверждённых центром больше storage.photo_retention_days дней назад — по умолчанию 30, 0 выключает; незагруженные не трогает никогда, метаданные и sha256 остаются в журнале агента — удаляется только копия файла, в центре снимок лежит 5 лет. Три бага найдены qa-tester и исправлены: пауза повторов переполняла float после ~1025 неудач; служебные времена сравнивались как ISO-строки с разными поясами; выключенный ретеншн завершал задачу, а выход ЛЮБОЙ фоновой задачи останавливает агента — служба с photo_retention_days = 0 умирала бы сразу после старта)
- [x] Панель: выгрузка журнала в CSV (11.08.2026; кнопка «Выгрузить» рядом с фильтрами: те же фильтры, BOM и «;» — Excel открывает двойным щелчком, потолок 20 000 строк, ограничение по объекту действует и здесь)
- [x] Панель: разграничение видимости по объекту — СДЕЛАНО 11.08.2026 (админ видит всё, остальные — свой объект; см. decisions); экран «Пользователи» показывает, кто чем ограничен (12.08.2026: подпись под привязкой — «видит только этот объект» / «видит все объекты» / «администратора привязка не ограничивает»; у отключённых учёток подписи нет — они не видят ничего; дополнен card-hint формы создания)
- [x] Панель: счётчики/алерты на дашборде (13.08.2026, вместе с мониторингом этапа 2: 4 счётчика по макету center-dashboard — «Весов на связи», «Взвешиваний сегодня» (бишкекские сутки), «Активных алертов», «Очередь досылки» — и блок «Активные алерты» с пилюлями типов / зелёная плашка «всё в порядке»; всё живое — обновляется тем же HTMX-опросом, что и сетка объектов)
- [x] **Печатная весовая карточка из агента и центра** (13.08.2026, запрос Игоря по образцу акта АИС «СВХ»; агент 0.4.9): общая печатная форма `shared/templates/card.html` + `shared/card.py` (агент и центр собирают контекст одной функцией build_card — номер, даты и строка поверки совпадают с обеих сторон), шапка ГТИ двуязычная БЕЗ банковских реквизитов, герб из PDF-акта (`shared/assets/gti-logo.jpg`, data:URI). Номер = `ВЕС-/ТАР-ГГГГММДД-ЧЧММСС` от бишкекского времени операции (сквозной счётчик не годится: агент печатает офлайн, а номер обязан совпадать при печати с обеих сторон). Одна форма на обе операции: у тарирования заполнена только ТАРА. Кнопки «Печать»: журнал агента и журнал центра (между «Нетто» и «Источником»), реестр тарирований (перед «Статусом»), страница записи; списки центра расширены (content--wide). Роут центра под PanelScope (чужой объект — 404), агента — под операторской сессией; страница сразу зовёт диалог печати. Фото: печатаются доступные; файл ещё не дослан с объекта (центр) или убран ретеншном при офлайне (агент) — вместо рамок честное предупреждение, где взять снимок. Дата тарирования у агента: локальная запись по uuid → фолбэк реплика реестра. СВИДЕТЕЛЬСТВО О ПОВЕРКЕ — в справочнике весов (страница «Настройки»: номер/дата/срок, миграция scales), агенту едет в снимке настроек (ScaleSettingsPayload.verification, старые агенты поле молча отбрасывают) — офлайн-печать полная; карточка печатает ОДНО свидетельство — весов записи (в акте АИС их два, потому что АИС не знает, какие из двух весов Канта взвешивали; мы знаем). ФИО ОПЕРАТОРА: у ручных операций уже писалось (вход оператора агента); для АИС-операций поле `operator` добавлено в контракт v1 (необязательное, до 200 символов) и протянуто АИС→центр→агент→запись→центр — заработает, когда АИС реализует запросы по нашему контракту (docs/contracts/ais-api-v1.md). Правки Игоря по виду (13.08, второй заход): шапка построчно вровень (названия жёстко в 3 строки + 4 строки реквизитов в обеих колонках), «БРУТТО, кг/ТАРА, кг/НЕТТО, кг», рамки фото ПЕРЕД/ЗАД печатаются всегда (недоступный снимок — пустая рамка), высота листа ~219 мм — А4 с запасом. 51 новый тест (1327 всего)
- [x] Панель: окно пагинации вместо перечня всех страниц (12.08.2026; общий макрос _pagination.html для журнала и тарирований: «1 … 4 5 [6] 7 8 … N» — текущая ±2 плюс края, многоточие, скрывающее ровно одну страницу, показывается её номером, номер из URL нормализуется в диапазон; тест окна на 11 страницах)
- [x] Панель, справочники: подписаны поля legacy-маршрута — «IP (маршрут АИС)» / «Порт» / «autoscale (№ весов)» в форме создания, title-подсказки на полях в строках таблицы, в card-hint предупреждение «Менять только синхронно со стороной АИС» и семантика «autoscale — номер весов на объекте, у одиночных АИС шлёт 1» (12.08.2026, урок сверки; упоминание «будущей страницы настроек» заменено ссылкой на существующую «Настройки»)
- [x] Панель: пагинация на экране «Тарирования» (11.08.2026; блок переключения страниц с сохранением фильтров и поиска)
- [x] Доставка логов агента, часть (а) (11.08.2026; вкладка «Диагностика» в интерфейсе оператора: состояние индикатора и порта, связь с центром, расхождение часов с центром, очередь записей, очередь снимков с числом застрявших, реестр тар, версия агента + хвост logs/agent.log с кнопкой обновления; читается только хвост файла, битые байты и незавершённая строка экран не ломают; работает без связи с центром)
- [x] Доставка логов агента, часть (б) (11.08.2026; log_tail_request/log_tail_response по WS, страница /panel/scales/{id}/log по кнопке «Журнал агента» на карточке весов — только админ, каждый просмотр в аудите; гейт версий ≥0.4.5, старым агентам панель объясняет, что нужно обновление)
- [x] Агент 0.4.6: таймаут съёмки камер — дефолт 15 с + наследование локального `timeout_s` камерами из scale_config (12.08.2026; урок Джалал-Абада — RTSP-камеры падали по 5-секундному таймауту из-за запуска ffmpeg с HDD, а снимок центра затирал локально поднятый таймаут; словарь таймаутов снимается с сырого config.toml до наложения снимка — замечание ревью; 1245 тестов, см. decisions)
- [x] Агент 0.4.7: постоянные потоки RTSP-камер с буфером последнего кадра, «как у UniServer» (12.08.2026, запрос Игоря; agent/cameras/stream.py + прокладка в превью/операции/фоновую проверку/настройки центра): живое превью у оператора, мгновенный снимок при взвешивании, фолбэк на разовую съёмку при обрыве потока; камерам со снапшотом (Кызыл-Кыя) поток не заводится. Блокер ревью закрыт: зависшее соединение обрывает `-timeout` rtsp-демьюксера (generic `-rw_timeout` для rtsp не действует — ревьюер доказал экспериментом); остановка потоков вынесена из-под лока менеджера. 1261 тест, см. decisions. **В БОЮ на обоих объектах** (кнопкой 12.08.2026; Джалал-Абад обновился при включённом 360 — белый список файла exe работает и по пути, замена exe при обновлении доверие не сбрасывает)

## Пожелания Игоря 14.08.2026

- [x] Панель: история тарирований машины — переключатель «Действующие / Вся история» на экране «Тарирования» (14.08.2026; реестр хранит одну строку на сцепку, история — только в журнале weighings: режим «Вся история» со статусами Действует/Истекло/Заменено и пометкой «Без номера ТС» у записей вне реестра, «Действует до» у заменённых — прочерк; поиск в обоих режимах ищет и по номеру прицепа; режим по умолчанию, расчёт нетто и репликация не изменились; 10 тестов, см. decisions)
- [x] Центр видит ВСЕ учётки агентов, включая заведённые на месте вручную (14.08.2026, агент 0.4.14; обратный канал operators_report: снимок local_users БЕЗ хешей паролей — после hello, после применения operators_registry и раз в 5 минут (ловит CLI add-operator, пишущий в SQLite другим процессом); таблица agent_operators (миграция e5f6a7b8c9d0), блок «Учётки на агентах» на экране «Пользователи», локальные помечены пилюлей «заведена на месте» — контроль обхода центра; порядок выката: центр РАНЬШЕ релиза агента (старый центр логировал бы отчёт как непонятное сообщение); 9 тестов, см. decisions. ВЫКАЧЕНО И В БОЮ 14.08: центр обновлён (миграция на боевой), релиз agent-v0.4.14 установлен кнопками на оба объекта, блок наполнился и сразу показал 2 локальные учётки — dzhaltest (Д-А) и admin (К-К, старый тестовый); блокировка местных из центра — кнопкой «Заблокировать» (сделана вторым витком, см. ниже); обе местные учётки перехвачены Игорем 14.08 — в снимках агентов теперь «из центра · отключена»)
- [x] Кнопка «Заблокировать» у местных учёток + фильтры экрана на блок «Учётки на агентах» (14.08.2026, второй виток; перехват логина: users_admin.block_agent_operator создаёт ОТКЛЮЧЁННОГО оператора-двойника со случайным паролем и привязкой к объекту весов, мгновенная рассылка снимка — «центр главнее» гасит местную учётку и офлайн; легитимные логины защищены: пользователи панели и операторы другого объекта — ошибка, действующий оператор этого объекта кнопкой НЕ отключается (снимок сам вернёт центровую поверх местной копии); поиск/объект/статус экрана действуют и на блок — роль и «— все —» к учёткам агентов неприменимы, пустые состояния различаются («под фильтры не попала» vs «агенты не прислали»); 13 тестов; релиз агента НЕ нужен — только выкат центра)
- [x] Кызыл-Кыя: роли камер перепутаны с установки — «ПЕРЕД» показывал хвост машины (14.08.2026, скриншот Игоря; камеры К-К жили только в локальном config.toml — заведены в справочник центра со свапнутыми ролями (front=.121, rear=.120), снимок уехал агенту на лету, мастер-папка ../ves-agent-0 поправлена; записи до ~14:00 14.08 несут старые роли фото — неизменяемость №2. ПОПУТНО НАЙДЕН БАГ (агент 0.4.15): применение камер из центра на лету доезжало до съёмки операций/ручного режима/проверки/потоков, но НЕ до превью оператора — оно снимало по замороженному config.toml до рестарта службы; на RTSP-объектах бага не видно (превью там из потоков, которые обновляются), стреляет только на снапшотных камерах. Фикс: SettingsManager.set_preview + AgentRuntime.set_cameras со сбросом кэша превью; 2 новых теста. Релиз agent-v0.4.15 установлен кнопками на оба объекта в тот же день — превью К-К подтверждено Игорем: «ПЕРЕД» показывает кабину)
- [x] Разобран вопрос «айпишник на весах может поменяться — что делать?» — разработка не потребовалась (14.08.2026): реальный IP весового ПК системе безразличен (агент подключается к центру исходящим WS по токену, центр реальных IP агентов не хранит и не проверяет — смена IP переживается молча); legacy-адрес в справочнике — ключ маршрутизации запросов АИС, правится на «Справочниках» только синхронно со стороной АИС (при рассинхроне АИС получает «Весы не найдены по указанному адресу», запрос ложится в audit_log); URL камер правятся в справочнике с доставкой агенту на лету, пропавшую камеру мониторинг поднимет алертом через 3 минуты. Возможная мелочь на будущее (к переключению АИС): событие мониторинга «запросы АИС не находят весы»
- [x] Взвешивание с устаревшим тарированием: показывать последнее тарирование — дату, время и МАССУ (14.08.2026, четвёртый виток дня, агент 0.4.16; одобренный план: только ИНФОРМИРОВАНИЕ — расчёт нетто, записи и контракт v1 НЕ тронуты, при устаревшей таре АИС по-прежнему получает tare/netto null + no_valid_tare, правило №4 нетронуто. Печатная карта: строка «Полная масса, кг:» снята — дублировала таблицу масс (предложение Игоря); на её месте примечание ТОЛЬКО у успешных взвешиваний без нетто: «тарирование сцепки от <дата время>, тара <масса> кг — устарело (тара действует 3 календарных месяца)» / «действующего тарирования сцепки не было» / «номер ТС не передан»; в таблице масс ТАРА остаётся прочерком — печатная таблица совпадает с записью и ответом АИС, масса устаревшей тары только в примечании. Страница записи центра: в боксе «Тара» ссылка на запись устаревшего тарирования с датой и массой. Агент: подсказка формы ручного режима показывает устаревшее тарирование ДО фиксации («нетто не будет рассчитано»), карточка результата называет дату и массу последнего. «Устарело» пишется только про истёкшее К МОМЕНТУ ВЗВЕШИВАНИЯ (общая граница правила №4 в UTC, shared.card.netto_note — обе стороны печатают одинаково): тарирования ПОЗЖЕ записи не показываются (старая карта не меняется задним числом после перетарирования), а не истёкшая, но не подставленная тара (реплика запоздала) устаревшей не зовётся. Формулировка хинта на «Тарированиях» заменена дословно формулировкой Игоря. По замечанию ревью реплика реестра тар агентам теперь едет ЦЕЛИКОМ (просроченные строки больше не фильтруются при репликации — иначе агентская печать теряла дату устаревшего тарирования после первого же обновления реплики; правило №4 держит читающая сторона, старые агенты лишние строки не подставляют), даты подсказок оператора переведены на местное время ПК (fmt_date — сырой UTC-strftime показывал вечерние тарирования вчерашним днём). 21 новый тест (1432); проверено глазами в dev-панели. ВЫКАЧЕНО И В БОЮ 14.08: центр обновлён, релиз agent-v0.4.16 установлен кнопками на оба объекта (четвёртый релиз за день, тихо) — в боевом реестре устаревших строк пока нет (пилот молодой), примечания начнут попадаться по мере старения тарирований)

## Этап 2 — тарирование в проде, офлайн, тираж 🔵

- [x] **Контракт v2 с АИС «СВХ» — СОГЛАСОВАН 17.08.2026 (версия 1.0)**: `docs/contracts/ais-api-v2.md` (docx/pdf для АИС — `../docs/Контракт интеграции с АИС «СВХ» v2 — …`, сборка docx из md — `../docs/справка/контракт-аис-v2/build_contract_docx.js`). За день: черновик 0.1 → два раунда ответов разработчиков АИС → 0.2 → 0.3 → подтверждение → 1.0 (история и все ответы — приложение А контракта, решения — decisions 17.08). Суть: `ais_ref` (номер документа АИС WEI…/TAR…, одна строка) — ключ идемпотентности; `ais_object` («Специальный идентификатор СВХ»: 0014 К-К, 0013 Д-А, 0002 Кант) + `scale_no` — привязка в нашем справочнике; онлайн — только синхронный REST с документом операции (тара во вложении со статусом, поверка, фото sha256, checksum); RabbitMQ — свой на ВМ, только офлайн-операции (`weighing.completed`), обратная связь номером АИС `POST /api/v2/weighings/{id}/ais_ref`; Bearer-токен + allowlist (192.168.140.150 бой / 192.168.6.116 тест); фото качает сервер АИС. Не вошло в 1.0: проверка тары до взвешивания, agent.status, weighing.reversed. architecture.md → v1.2
- [x] **Реализация контракта v2, виток (а): API v2** (17.08.2026): `center/api_v2/` — `POST /api/v2/weighings` (валидация схемой WeighV2Request с телом 422 `{code: ERR_VALIDATION, details}`; маршрут по паре «Специальный идентификатор СВХ + № весов» → поля `scales.ais_object`/`ais_scale_no` (миграция f6a7b8c9d0e1, частичный уникальный индекс, форма и колонка на «Справочниках», CLI `--ais-object/--ais-scale-no`, демо-посев); Bearer-токен интегратора (тот же `AIS_PHOTO_TOKEN`) + `AIS_ALLOWED_IPS`; исходы операции — 200 + code, 401/403/404/422 только для неверного запроса; аудит `weigh_request_v2`), ИДЕМПОТЕНТНОСТЬ по `ais_ref`: таблица `weighing_ais_refs` (weighing_id↔ais_ref UNIQUE, origin command|callback), номер пишется В ОДНОЙ ТРАНЗАКЦИИ с записью — хаб помнит ais_ref по request_id (`remember_ais_ref`/`take_ais_ref`, TTL 15 мин для поздних результатов), WS-роутер передаёт его в `save_weighing_record`; повтор по состоявшейся → тот же документ `repeated: true`; повтор во время выполнения → ждёт исход первой (inflight-future, регистрация до похода в БД); после отказа → новая попытка; занятый номер при сохранении не роняет транзакцию (запись без связки + warning). ДОКУМЕНТ ОПЕРАЦИИ (`center/api_v2/documents.py`, один на ответ/событие/GET) строится ИЗ ЗАПИСИ: тара во вложении со статусом applied (из записи) / expired / not_applied по журналу (`repo.latest_taring_as_of` — последнее тарирование сцепки не позже момента взвешивания, граница №4 в UTC), поверка весов, фото с `available`, checksum. `GET /api/v2/weighings/{id}`, `?ais_ref=`, список за период (from/to/ais_object/scale_no/operation/source/unlinked, пагинация ≤200) и обратный вызов `POST /api/v2/weighings/{id}/ais_ref` (200/404/409/422, `repo.link_ais_ref`) — тоже готовы. nginx: location /api/v2/ с allow для 192.168.140.150/192.168.6.116/рабочего места. 46 новых тестов (1479). ИЗВЕСТНОЕ ОГРАНИЧЕНИЕ (ревью): связка ais_ref живёт в памяти хаба до результата — при рестарте центра во время операции или разрыве WS (запись придёт offline_sync без request_id) запись `source=ais` сохранится без номера, повтор АИС запустит второе взвешивание; закрыть в агенте 0.4.17 — `ais_ref` в `WeighRequest` с эхом в `WeighingRecord` (тогда номер едет с записью любым путём), заодно передача действующей тары в weigh_request
- [x] **Реализация контракта v2, виток (б): outbox и RabbitMQ** (17.08.2026): миграция a7b8c9d0e1f2 — таблица `weighing_events` (outbox); `repo.save_weighing_record` ставит `weighing.completed` В ТОЙ ЖЕ ТРАНЗАКЦИИ для записей `source=local_offline` (онлайн-операции событиями не публикуются — их исход АИС берёт из HTTP-ответа); `center/events.py` — `EventPublisher` (фоновая задача lifespan: порция из outbox → publish с подтверждением брокера → published_at; ошибка публикации прерывает порцию и уходит в backoff до 60 с, ошибка сборки одного события помечается на строке и порцию не блокирует; без `RABBITMQ_URL` выключен, outbox копится) и `RabbitBroker` (aio-pika, connect_robust, publisher confirms, таймауты; топология — exchange `vesy.events` topic + очередь `ais-svh.weighings` + привязка `weighing.completed.#` объявляется центром; сообщение persistent, message_id = event_id (uuid5 по операции и типу — повтор несёт тот же id), type, app_id); конверт `{event_id,type,version,published_at,ais_object,weighing}`, routing key `weighing.completed.<ais_object>` (`.unbound` у весов без привязки). Мониторинг: детектор `ais_events` (danger, порог 10 мин от постановки самого старого неотправленного события, по весам операции). Панель: страница записи — блок «АИС «СВХ»» (номер документа / статус события / ошибка — только админу) и кнопка «Переотправить событие» (админ, `POST /panel/journal/{id}/ais_event` → новая строка outbox + аудит; допускается и для онлайн-записей — 7.4 контракта), счётчик дашборда «очередь досылки» включает неотправленные события в АИС. Deploy: сервис `rabbitmq` в compose (rabbitmq:4.1-management-alpine, vhost `vesy`, учётка `vesy-center` из `RABBITMQ_PASSWORD`, 5672 наружу, 15672 только на 127.0.0.1; app от брокера НЕ зависит), `RABBITMQ_URL` у app, `.env.example`, deploy/README §2а (учётка `ais-svh` только на чтение через rabbitmqctl, ловушки: RABBITMQ_PASSWORD до первого выката, консьюмер без queue.declare, порт минует ufw). 16 новых тестов (1496)
- [x] **Реализация контракта v2, виток (в): панель и фото** (17.08.2026): обратный вызов `POST /api/v2/weighings/{id}/ais_ref` и показ номера АИС на странице записи/во вложении `tare.ais_ref` сделаны ещё в (а)/(б); добавлено: фильтр журнала «Офлайн без номера АИС» (`source=local_offline_unlinked` — офлайн-операции, по которым АИС ещё не сообщила номер, 7.5 контракта); фото интеграторам — `ETag` = sha256 из записи, `Cache-Control: private, max-age=31536000, immutable`, `If-None-Match` → 304 (миниатюры — с кэшем, без sha-ETag). Проверено глазами в dev-панели: блок «АИС «СВХ»» на записи, кнопка переотправки с заметкой, колонка «Привязка АИС (v2)» на «Справочниках». 2 новых теста (1498)
- [x] **Реализация контракта v2, виток (г): инструменты и e2e** (17.08.2026): `tools/ais_client_v2.py` — имитатор АИС для v2 (подкоманды weigh/get/find/list/link, токен из --token или env AIS_TOKEN, stdlib), `tools/ais_consumer.py` — эталонный консьюмер (aio-pika, `get_queue(ensure=False)` — без объявления очереди, prefetch 10, ack после обработки, дедуп по weighing.id, пароль в выводе маскируется); сквозной тест `test_e2e_stage1` дополнен шагом v2 (команда с ais_ref на живом агенте+эмуляторе → документ с тарой во вложении, повтор → repeated, GET по номеру); ИНТЕГРАЦИЯ С НАСТОЯЩИМ RabbitMQ ПРОВЕРЕНА локально (rabbitmq:4.1-management-alpine в docker: RabbitBroker объявил топологию и опубликовал 2 события, консьюмер под учёткой ais-svh только с правом read забрал и подтвердил их, очередь опустела). 4 новых теста (1502)
- [x] **Реализация контракта v2, виток (д): выкат на ВМ** (17.08.2026, ночь): в `deploy/.env` на ВМ добавлен `RABBITMQ_PASSWORD`; `ship.sh` — образ с aio-pika, миграции f6a7b8c9d0e1 + a7b8c9d0e1f2 применены (alembic head a7b8c9d0e1f2), поднят контейнер `rabbitmq` (healthy, 5672 наружу, 15672 локально), app пересоздан с `RABBITMQ_URL`; nginx пересоздан (location /api/v2/ с allow 192.168.140.150 / 192.168.6.116 / 192.168.15.9); публикатор при старте объявил топологию (exchange `vesy.events`, очередь `ais-svh.weighings` — проверено rabbitmqctl); заведена учётка `ais-svh` только на чтение очереди (пароль — на ВМ в `~/rabbit-ais-svh.txt`, в git его нет); боевые весы привязаны: Кызыл-Кыя → 0014/1, Джалал-Абад → 0013/1 (UPDATE scales по кодам объектов). ПРОВЕРЕНО С МАКА: `tools.ais_client_v2 list` через nginx с токеном АИС из .env ВМ → 200 с документом операции; `tools.ais_consumer` под учёткой ais-svh дошёл до боевого брокера и очереди (событий пока нет — офлайн-операций в пилоте не было). СДЕЛАНО 18.08: доступы АИС переданы (письмо с адресом, токеном, строкой RabbitMQ, контрактом и образцами `tools/ais_client_v2.py`/`tools/ais_consumer.py`); по их просьбе открыты рабочие места 192.168.15.10/.2/.4. ЗА ИГОРЕМ: (2) системщику — доступ с 192.168.140.150 (бой) и 192.168.6.116 (тест) до 192.168.140.70 по HTTP и 5672 (порт публикует докер — если режут ufw, правило в DOCKER-USER); (3) `AIS_ALLOWED_IPS` в .env ВМ сейчас пуст — при подключении АИС вписать их адреса и `docker compose up -d --force-recreate app`. Расширения по желанию АИС — раздел 12 контракта
- [x] **Агент 0.4.18** (18.08.2026): (1) сторожок автообновления докладывает центру — `AgentUpdater.notify` → `CenterClient.post_message` (очередь исходящих уходит с ближайшим heartbeat, переживает реконнект, снимается только после успешной отправки), центр превращает `update_status(ok=False)` в событие мониторинга `update_failed` (warning → «События» панели и Telegram; на дашборд-алерты не попадает — это одноразовое событие); (2) подмена папки в bat с шестью повторами по 5 с (урок Проводника); (3) СЕССИИ СЛЕДУЮТ ЗА ПАРОЛЕМ (вопрос Игоря 18.08 — учётку давали другому человеку): панель — штамп пароля `sha256(pw_hash)[:16]` в cookie при входе, `users_admin.session_state` одним запросом на каждый HTTP-запрос (активна/админ/объект/штамп; кэш в request.state), несовпадение или отключение → сессия очищена и на вход; смена своего пароля админом сессию не рвёт; интерфейс оператора агента — `operator_stamp` в сессии, сверка на каждом запросе и при подключении /ws/state (смена пароля через реплику центра или блокировка выбивают вошедшего). Побочный эффект после выкатки: все живые сессии панели и операторов выбьются ОДИН раз (cookie без штампа). 9 новых тестов (1528). ВЫКАЧЕНО 18.08: центр обновлён (app healthy), релиз `agent-v0.4.18` на ВМ (sha256 93b15440020cafd533537038850041787de43615808ca4b36fc638a3b2fd7acf), мастер-папки обновлены; ОБА АГЕНТА ОБНОВЛЕНЫ КНОПКАМИ 18.08 (тихо). Тогда же по просьбе разработчиков АИС `/api/v2` в nginx открыт для их рабочих мест 192.168.15.10, 192.168.15.2, 192.168.15.4 (nginx пересоздан, проверено с мака)
- [x] **Агент 0.4.17: номер документа АИС в записи + действующая тара в команде агенту** (18.08.2026): `WeighRequest.ais_ref/tare/tare_resolved`, `WeighingRecord.ais_ref` (shared/messages); агент — колонка `weighings_local.ais_ref` (ALTER TABLE для старых БД, триггер неизменяемости пересоздан по метке, самолечение триггеров/индексов теперь безусловное на каждом старте), авторежим при `tare_resolved` берёт тару из команды (в т.ч. «нет тары») и не ходит в реплику, ais_ref пишет в запись и эхом в отказы; центр — WS-роутер берёт номер из записи (проверка формата, расхождение с памятью хаба — в лог; память чистится всегда), offline_sync с ais_ref тоже закрепляет номер, `repo.resolve_tare_hint` + подсказка тары в командах v1 и v2. Закрыто ограничение ревью 17.08 (связка в памяти хаба). 17 новых тестов (1519). ВЫКАЧЕНО: центр на ВМ обновлён 18.08 (подсказка тары в командах, номер из записи агента), релиз `agent-v0.4.17` собран CI и положен на ВМ в `deploy/releases` (sha256 011f8a922cc59a840ea14328f0fca5e92d0850f9c98fa9dcbb5532024421edc0), мастер-папки `../ves-agent-0/app` и `../ves-agent-jalalabad/app` обновлены ditto (816 файлов); ОБА АГЕНТА ОБНОВЛЕНЫ КНОПКАМИ 18.08 (К-К сразу; Д-А — со второй попытки после двух боевых ловушек: 360 резал дочерние tar/schtasks при целом белом списке — Игорь добавил папку в «Исключения»; затем `move app` сорвался из-за открытого в Проводнике `D:\vesy-agent\app` — служба перезапустилась старой версией; повтор кнопки прошёл). Уроки — install-agent-windows.md «Антивирус». В КОД (в следующий релиз, 0.4.18): подмена папки в bat с шестью повторами по 5 с (сделано, тесты); в бэклог — сторожок агента должен докладывать центру «bat не перезапустил службу» (сейчас только agent.log)
- [x] Автообновление: распаковка app_new перенесена из процесса агента в self-update.bat планировщика (12.08.2026, агент 0.4.8; боевой урок 360 — антивирус не даёт неподписанному ves-agent.exe писать системные DLL. Агент теперь только скачивает, сверяет sha256/размер и проверяет оглавление архива на zip-slip БЕЗ распаковки, оставляя проверенный update.zip; bat распаковывает системным tar → фолбэк PowerShell Expand-Archive → и только затем останавливает службу (неудача распаковки оставляет агента работать). На ПК без tar/Expand-Archive (Windows 7) — прежняя распаковка агентом; сторожок: живой процесс через 15 мин после запуска bat пишет ошибку в agent.log. Механизм впервые сработает при обновлении С 0.4.8 — само 0.4.7→0.4.8 идёт старым кодом; см. decisions. **Релиз agent-v0.4.8 выпущен и установлен кнопкой на оба объекта 12.08.2026 вечером** — при следующем релизе проверить в logs/update.log Джалал-Абада, что распаковку сделал tar при включённом 360)
- [x] Панель: cookie сессии с https_only (флаг Secure) через env-переключатель (13.08.2026; PANEL_COOKIE_SECURE=1, по умолчанию ВЫКЛЮЧЕН — на пилоте в панель ходят и по внутреннему http://192.168.140.70/panel/, с флагом такой вход сломается; предупреждение в .env.example)
- [x] Панель: «Журнал агента» переведён с GET на POST-кнопку (13.08.2026; риск lax закрыт: кнопки на карточке дашборда и «Запросить снова» — POST-формы, GET отвечает 405 и команду агенту не шлёт)
- [ ] Подписывать сборки агента сертификатом (code signing) — снимет недоверие антивирусов к ves-agent.exe (тот же урок 12.08.2026); вопрос покупки сертификата — к Игорю к началу тиража
- [x] Автообновление: окно busy→stop сужено обратно до секунд (13.08.2026, агент 0.4.13; распаковку ДО ожидания весов делает сам агент ДОЧЕРНИМ процессом системного tar (фолбэк Expand-Archive) — писатель файлов доверенный, урок 360 соблюдён, bat получает только подмену папки. Не вышло (антивирус срезал инструмент из-под неподписанного родителя?) — автоматический фолбэк на боевой путь 0.4.8: update.zip остаётся, распаковывает bat планировщика; совсем без инструментов — прежняя распаковка агентом. Заработает на обновлении С 0.4.13 на следующую версию — 0.4.12→0.4.13 идёт старым кодом. ПРОВЕРЕНО 14.08.2026 на релизе 0.4.14: лог Джалал-Абада — «релиз распакован доверенным инструментом до остановки службы» при включённом 360, новый путь боевой; на Кызыл-Кые строка успела вытесниться из 200-строчного хвоста запросами превью, обновление прошло успешно)
- [x] **Автовыкат агентов по каналам pilot → stable, самопроверка и автооткат** (18.08.2026, architecture §7а; агент 0.4.19; см. decisions): центр — таблица `agent_releases` ожила (канал nullable, описание, кто/когда назначил; артефакт по-прежнему файл в каталоге релизов: scp или ЗАГРУЗКА ЧЕРЕЗ ПАНЕЛЬ с проверкой оглавления и атомарной записью), журнал раскатки `agent_updates` (строка на пару агент+версия: commanded → started → installed | failed | rolled_back; миграция b8c9d0e1f2a3), движок `center/rollout.py` — фоновая задача центра раз в 30 с шлёт update_command агентам на связи с версией ниже релиза их канала (pilot без своего релиза берёт stable; только вверх; не больше 3 одновременно; отказы повторяет до 3 раз через полчаса; откат терминален до кнопки «Повторить»); переходы по hello (целевая версия = installed; прежняя после installed = откат без доклада) и по update_status со стадиями (0.4.19); исходы — события «Обновление» (успех ok, отказ/откат warning → «События» и Telegram); кнопка дашборда «Обновить до vX» теперь показывает цель КАНАЛА и пишет в тот же журнал (manual). Агент 0.4.19: `update-context.json` перед bat, bat после старта службы ждёт `update-check.ok/.fail` до 6 мин и при провале/молчании откатывает (app → app_failed, app_old → app, `update-rollback.txt`), самопроверка новой версии (`agent/selfcheck.py`: веб-порт, связь с центром, индикатор — если шёл до обновления) с докладом installed, прежняя версия после отката докладывает rolled_back с причиной, чужой контекст — «не состоялось»; UpdateStatus.stage/running_version. Deploy: том релизов rw, nginx 64m. 75 новых тестов (1603); правки по ревью: гард отката без папки app, ожидание самопроверки перед новым обновлением (агент) и окно устаканивания 10 мин (центр), события по hello только для агентов до 0.4.19, «уже выполняется» — не отказ, атомарная публикация файла (os.link), канонические имена. Механизм самопроверки включается при обновлении С 0.4.19 (bat пишет обновляющийся агент). ВЫКАЧЕНО 18.08 (коммиты 9c72401, 3ef0f6a, CI зелёный): центр на ВМ обновлён (миграция b8c9d0e1f2a3 применена, том релизов rw, nginx 64m), релиз `agent-v0.4.19` собран CI и положен в каталог ВМ (sha256 8444bb91cc90b72faa6f0271808c503c731b4fa6b2571e1ca0eeb9a874fd48b3, sha на ВМ сверен), мастер-папки `../ves-agent-0/app` и `../ves-agent-jalalabad/app` обновлены (816 файлов); каналы боевых агентов разведены для первого автовыката: К-К (id=1) — pilot, Д-А (id=2) — stable (через refs_admin.set_agent_channel на ВМ). ПЕРВЫЙ АВТОВЫКАТ ПРОШЁЛ 18.08 (кнопки Игоря на экране «Релизы»): «В pilot» → К-К получил команду 10:09:21 UTC и подтвердил 0.4.19 через 14 с; «Перевести в stable» 10:09:53 → Д-А команда 10:10:21, подтверждение 10:10:36; журнал agent_updates — обе строки installed/auto/1 попытка, ОБА АГЕНТА НА 0.4.19. Событий «Обновление» этот переход не дал — ожидаемо: подтверждение шло по hello (для 0.4.19+ события по hello не пишутся), а самопроверки при переходе СО старой версии ещё нет (bat писал 0.4.18). СЛЕДУЮЩИЙ РЕЛИЗ (0.4.20) — первый с самопроверкой и автооткатом: проверить ok-событие «самопроверка пройдена», строки «ждём самопроверку» в logs\update.log и отсутствие маркеров update-check.* после установки
- [x] Мониторинг: «Состояние объектов» + алерты Telegram (13.08.2026, одобрено Игорем; email — вторым каналом позже, если понадобится. center/monitoring.py: детекторы фоновой задачей процесса центра (lifespan, один воркер uvicorn) раз в 30 с — агент офлайн ≥5 мин (полуживой TCP со stale heartbeat ≥2 мин считается офлайном), индикатор не отдаёт вес ≥3 мин, камера недоступна ≥3 мин (по ролям), очереди досылки записей/снимков не рассасываются ≥10 мин, мало места на диске (<5 ГБ — сразу). Антидребезг порогами; активные алерты в памяти (после рестарта восстанавливаются за тик), события переходов (появилась/закрылась) — в monitoring_events (миграция d4e5f6a7b8c9). У офлайн-агента детекторы оборудования замирают — алерт камеры не закрывается «за отсутствием данных». Агент 0.4.13: heartbeat дополнен pending_photos_count и disk_free_mb (необязательные поля — старые агенты молчат, детекторы по ним молчат). TelegramNotifier: секреты TELEGRAM_BOT_TOKEN/CHAT_ID из env (без них выключен, события копятся в журнале), доставка по одному с отметкой notified_at (недоступный Telegram не теряет события), окно доставки 6 ч против потопа древних алертов. Панель: вкладка «События» (/panel/events: фильтр по объекту, пагинация окном, PanelScope) + счётчики и блок алертов на дашборде (см. бэклог этапа 1). 39 новых тестов (после правок ревью — 1376 всего). Закрыто в тот же вечер целиком: токен бота получен, центр выкачен, релиз agent-v0.4.13 выпущен и установлен кнопками на оба объекта. БОЕВОЙ УРОК 14.08.2026: Telegram-группа мигрировала в супергруппу — chat_id сменился (-5485263200 → -1004389188992), sendMessage падал 400, первые настоящие события (индикатор Д-А, 09:54–09:56) копились недоставленными, в логе голый «HTTP Error 400» без причины; вылечено правкой TELEGRAM_CHAT_ID в deploy/.env + force-recreate app — события доехали задним числом в окне 6 ч (механика «не теряем» отработала), тело ответа Telegram (description + migrate_to_chat_id) теперь попадает в лог. Тем же вечером алерты ПЕРЕВЕДЕНЫ ИЗ ГРУППЫ В КАНАЛ «Алерты весов ОАО "ГТИ"» (chat_id -1004475491902, бот — админ с правом постить; решение Игоря: id канала стабилен, миграций как у групп не бывает) — доставка в канал проверена)
- [x] Экран «Релизы агентов» в панели (18.08.2026, макет center-releases, только админ): «Загрузить новый релиз» (zip из GitHub Actions), таблица «Версии» (дата, канал-пилюля pilot/stable/архив/не назначен, sha256, размер, описание с правкой, сколько агентов на версии и сбоев, кнопки «В pilot» / «В stable» / «Перевести в stable» / «Отозвать»), блок «Раскатка» по агентам (канал переключается тут же, текущая версия, цель, статус установки с причиной, обновлено, «Обновить сейчас»/«Повторить»), подсказка «в pilot N дней»; вкладка «Релизы» в шапке для админа. Проверено глазами в dev-панели
- [x] **СВХ «Алтын логистик» — обследование ПЯТОГО объекта, ПРОТОКОЛ ВСКРЫТ ЗА ВЕЧЕР** (26.08.2026): выгрузка UniServer разобрана (`../docs/справка/выгрузка-алтын/`, карточка `docs/objects/altyn-logistik.md`), код АИС **0018**, весовой ПК 192.168.174.240:8087 (autoscale 1; второй IP 192.168.0.50 на той же сетевухе), поверка №217 от 10.06.2026 (до 10.06.2027). Индикатор — **Keli D2008** (шильдик снят в тот же день; пресет UniServer «D2008F»). Живой дамп через SERIALTCPIP1 Mode=Client → **приёмник на ВМ центра** (к рабочему месту сеть объекта НЕ пускает — политика «с объекта только к центру»; ping ходит, TCP нет — проверять Test-NetConnection; разгадка «не подключается» — Игорь не переключил Mode в Client, урок: проверять сам режим, «Выполняется» горит и у сервера): 4020 пакетов, 0 битых КС, ~5 пак/с, значения `+0000000`→`+0000600` при «60» на табло (оператор встал на весы). **ПРОТОКОЛ = «VESAR» БАЙТ В БАЙТ — новый драйвер НЕ нужен, подходит готовый `vesar`** (Keli D2008 — вероятный оригинал «VESAR»-клона К-С; спека vesar.md дополнена). Индикатор сам квантует поток к 20 кг (разгадка кратности журнала при настройке UniServer 10) → сверка с UniServer — СТРОГОЕ совпадение; пороги 400/800. Камеры 4К .174.20/.21 — **ISAPI-снапшоты 101/102 проверены с рабочего места** (ffmpeg не нужен, превью есть), **часы камер ВЕРНЫЕ** (первый объект без правки NTP). ~23 док/день, круглосуточный; все операции — разовые брутто вручную. Весы — «Пегас» 22-80-А-10, НПВ 60 т (второй «Пегас» в пилоте, но с Keli вместо CAS). **В центре ЗАВЕДЕНО в тот же вечер** (скрипт provision_altyn.py, репетиция на dev; боевой прогон выполнил Игорь — классификатор блокировал мою запись в боевую БД): объект id=6, весы id=5 «Весы D2008» (vesar, АИС 0018/1, legacy пуст — v1 не будет), пороги 400/800, поверка №217, камеры со снапшотом+превью, агент `altyn-1` (pilot), токен на ВМ и в конфиге; подпись «Keli D2008 (Пегас 22-80-А-10, 60 т)». Мастер-папка `../ves-agent-altyn/` + zip (app 0.4.24 из релиза ВМ, УСТАНОВКА-АЛТЫН.md). **УСТАНОВЛЕН И ОНЛАЙН В ТОТ ЖЕ ВЕЧЕР** (ставил Игорь; диска D: на ПК не оказалось — конфиг/инструкция/zip переведены на `C:\vesy-agent` по ходу): служба «ves-agent», агент 0.4.24 онлайн с 20:15, SERIALTCPIP1 возвращён в Server/127.0.0.1, веб 8090 отвечает, алертов индикатора/камер нет. Осталось: роли камер по заезду, сверка (СТРОГОЕ совпадение), учётка весовщика, d/e с шильдика
- [x] **ПЗТК «Беловодское» — предварительная оценка ШЕСТОГО кандидата (заочно, по фото)** (27.08.2026; карточка `docs/objects/belovodskoe.md`): весы автомобильные **SCS-80** зав. 00000068, **НПВ 80 т, e=20 кг** (первые 80-тонные), принадлежат Филиалу ЗАО «UBS Transit»; поверка №6673 от 19.09.2025 — ⚠️ истекает 19.09.2026, к внедрению нужна свежая. Индикатор **Yaohua XK3190-D2+**. Вопрос Игоря «менять ли индикатор вместе с ПК» — **ответ: НЕ менять, интегрируемся**: RS232 к ПК штатный с режимами непрерывной передачи (мануал D2+), формат семейства XK3190 открыт и примитивен (реверс `=`+7 цифр как A9 или 12 байт с XOR как A12 — подтвердим дампом), а главное — на объекте вес УЖЕ идёт в ПО по этому порту. Отличие от всех прошлых объектов: **UniServer-а НЕТ** — взвешивают «ProjectBalance 2008» («System of Weighing V8.0», штатное ПО Yaohua) на **Windows XP** → выгрузки конфига не будет, дамп снимем напрямую (утилита чтения COM на новом ПК или ноутбук с USB-RS232), замена ПК обязательна и так (агент на XP не встанет — объект новый ПК уже планирует). Внедрение отложено до установки ПК; понадобится драйвер **xk3190** (третий; заодно вынести DISCRETE_KG в конфиг)
- [x] **Очистка тестовых операций на бою** (02.09.2026, по просьбе Игоря — все взвешивания/тарирования до переключения АИС были тестовыми; пользователи и справочники не тронуты): удалена 81 запись `weighings` (весы 1–4, 10.08–27.08, все source=ais), 150 фото (+150 миниатюр, 108 МБ на томе photos), 51 привязка `weighing_ais_refs`, 12 событий outbox `weighing_events`, 3 тары `tare_registry`; `audit_log` (573) и `monitoring_events` оставлены. Триггеры неизменяемости `weighings_immutable`/`weighing_photos_immutable` отключались ТОЛЬКО на время одной транзакции и включены обратно (проверено по pg_trigger). Перед чисткой — бэкап на ВМ: `~/backups/ves-before-cleanup-2026-09-02.dump` (pg_dump -Fc, 16 таблиц) и `~/backups/photos-before-cleanup-2026-09-02.tgz` (300 файлов). После чистки `docker compose restart app` — агенты при переподключении получили ПУСТОЙ реестр тар (иначе локальная реплика подставила бы тестовую тару в нетто; прямой SQL push не делает — урок Кара-Суу). Старое агенты не перешлют: досылка только записей с synced=0, все фото были загружены (150 = 300/2). НЕ чищено: локальные журналы агентов (SQLite на ПК объектов — триггеры запрещают удаление, только остановка службы + удаление файла БД руками через AnyDesk). ПОБОЧНАЯ НАХОДКА: ежедневный бэкап БД из deploy/README (cron → /backup) на ВМ НЕ НАСТРОЕН — ни каталога, ни задания; предложено Игорю
- [x] **СВХ «Кокчо-Коз» — заочное обследование, СЛЕДУЮЩИЙ объект к запуску** (02.09.2026; карточка `docs/objects/kokcho-koz.md`): ОсОО «Көкчө Көз» (терминал таможни), одни весы, АИС **0021**/№1, Windows 10, IP ждём (системщик настраивает сеть и маршрут). Индикатор **Yaohua XK3190-DS3** (для цифровых датчиков; RS232/RS422 600–9600 `bt`, режимы `tF`: 0 — непрерывно, 1 — по запросу; формат кадра подтвердим дампом) — то же семейство, что D2+ Беловодского → **драйвер `xk3190` пишем под Кокчо-Коз первым**. Поверка №857 (Ош) 24.06.2026 → 24.06.2027, до 60 т, e не указана. **UniServer-а НЕТ — «ВесыСофт: Весовой терминал»** (только RS-232, TCP не умеет; ≈15 операций за утро) → ретрансляция порта нашими средствами: план A — **com0com (2 пары) + hub4com службой nssm** (физический COM → ВесыСофт двунаправленно + агент на чтение), B — Y-кабель RS232, C — агент-ретранслятор (запас). Дамп — напрямую с COM новым **`tools/serial_dump.ps1`** (PowerShell без зависимостей; порт должен быть свободен — снимать с виртуального порта после hub4com или закрыв ВесыСофт). Чек-лист Игорю — в карточке и «Вопросах»
- [x] **Место на диске весового ПК из центра: срок хранения локальных фото + кнопка «Освободить место», агент 0.4.25** (02.09.2026; вопрос Игоря «нужна ли команда удаления взвешиваний/тарирований за период, чтобы чистить диск ПК удалённо» — ответ НЕТ: записи не удаляются нигде (правило №2, на агенте это запрещают триггеры), диск занимают только файлы снимков; см. decisions 02.09): поле «Хранить локальные фото, дней» на странице «Настройки» весов (`scales.photo_retention_days`, миграция a3b4c5d6e7f8; пусто — локальный конфиг, 0 — не убирать) → снимок настроек `ScaleSettingsPayload.photo_retention_days` → агент применяет НА ЛЕТУ (`PhotoRetention.set_retention_days` + будильник цикла — уменьшенный срок действует сразу, в т.ч. если пришёл во время уборки) и при старте (`merge_center_settings`); цикл ретеншна теперь живёт всегда. Команда `photo_cleanup_request` → агент убирает ВСЕ уже принятые центром снимки (`cleanup_now`: только uploaded=1, без ограничения порций, метаданные остаются, неотправленные не трогаются, работает и при выключенной плановой уборке) → `photo_cleanup_response` (файлов, байт с миниатюрами, свободно на диске; ошибка текстом — пустой текст исключения заменяется именем класса). В панели: кнопка «Освободить место» на карточке весов (только админ, только агент 0.4.25+ на связи — гейт `supports_photo_cleanup`; тайм-аут хаба 150 с < proxy_read_timeout nginx), итог в заметке дашборда, каждая команда — в `audit_log` `agent_photo_cleanup` (и неудачная — с текстом ошибки); строка «Диск ПК: свободно … ГБ · не отправлено фото: N» на карточке из heartbeat (красная ниже порога алерта). Тесты: +54 (сообщения и гейт версии, cleanup_now/срок на лету/будильник, merge и менеджер настроек, ws_client, хаб и диспетчер центра, снимок настроек, сохранение и маршруты настроек, панель — кнопка/аудит/гейт/офлайн/диск), всего 1910. Ревью: 4 замечания закрыты (будильник во время уборки, тайм-аут vs nginx + аудит неудачных команд, пустой текст исключения, PROGRESS). **ВЫКАЧЕНО 02.09.2026**: центр — ship.sh (миграция a3b4c5d6e7f8 применена, app healthy, все 5 агентов переподключились и онлайн); релиз agent-v0.4.25 собран GitHub Actions, zip выложен в `~/vesy-gti/deploy/releases/` на ВМ (sha256 сверен), app в мастер-папках всех объектов заменён на 0.4.25. РАСКАТКА — за Игорем экраном «Релизы» («В pilot» → наблюдение → «Перевести в stable»); до раскатки кнопка «Освободить место» агентам 0.4.24 не показывается, поле срока они игнорируют
- [x] **Агент 0.4.26 — журнал после уборки фото не долбит центр; вёрстка карточки весов** (02.09.2026 вечер, после раскатки 0.4.25 Игорем на все 5 объектов «В pilot»): на Кызыл-Кые после «Освободить место» журнал агента показал прочерки — снимки записей от 21.08 в центре удалены утренней чисткой (ожидаемо), но агент за 4 ч сделал 2158 запросов к центру с 404 (журнал переклеивается каждые 5 с, отказ не помнился). Сделано: `PhotoLibrary` помнит 404 центра 10 мин (`MISSING_TTL_S`, часы инъекцией), миниатюру из центра кладёт на диск рядом с убранным кадром (полный кадр не тянет), роут `/photos/…` отдаёт 404 с max-age=300; CSS `.stat > span:not(.dot)` вместо `span:last-child` (текст в строках с кнопкой был крупным и в три строки — замечание Игоря; дефект жил и в строке «Агент … · Журнал агента»), строка «Диск ПК: 143,8 ГБ» + кнопка «Освободить» (замер: в карточку 300 px влезает только так), «Не отправлено фото: N» отдельной строкой при N>0, разделитель тысяч. Вёрстка проверена в браузере на dev-панели (статичная карточка). Тесты +8 (память об отказах/TTL/500 не помним/миниатюра на диске/полный кадр не сохраняем/кэш 404/полный кадр под видом миниатюры ужимается/мусор не хранится), всего 1917. Ревью: pop вместо del на просроченном ключе (гонка потоков роута), центр без миниатюры отдаёт полный кадр — ужимаем копию перед хранением, лог отказа один раз, страховка от роста памяти отказов, `_write_atomic` общий. См. decisions 02.09 (вторая запись). **ВЫКАЧЕНО 02.09.2026 вечером**: центр — ship.sh (новая вёрстка карточек на бою, все 5 агентов онлайн), релиз agent-v0.4.26 собран и выложен в `~/vesy-gti/deploy/releases/` (sha256 сверен), мастер-папки на 0.4.26. **РАСКАТАН Игорем в тот же вечер**: К-К, Аламедин, Кара-Суу, Алтын — 0.4.26 онлайн; релиз переведён в stable; Джалал-Абад (канал stable) офлайн с 21:23 по Бишкеку (ПК объекта выключают на ночь, как 20.08) — догонит 0.4.26 сам при выходе на связь
- [ ] Драйверы для остальных статических объектов по выгрузкам UniServer (Пегас, Соболь, другие CAS/Keli; Keli D2008 покрыт драйвером vesar — 26.08.2026; драйвер xk3190 для Кокчо-Коза (DS3) и Беловодского (D2+) — обследования 02.09/27.08.2026)
- [ ] Установка по объектам, пообъектный вывод UniServer
- [x] **ПЗТК «Джалал-Абад» — второй объект пилота, подготовка** (12.08.2026): выгрузка UniServer изучена (`../docs/справка/выгрузка-джалалабад/`) — индикатор вещает **тот же CAS 22 byte** (по реестру весы ВМС «Пегас», но отдельный драйвер НЕ нужен; порт COM3, дискретность 20 кг → пороги 400/800), табло/RFID/распознавание в UniServer не подключены. В центре заведены объект `jalal-abad`, весы id=2 (legacy-маршрут 192.168.154.100:8087, autoscale=1), камеры front/rear (только RTSP: камеры не Hikvision, снапшота нет → в комплект добавлен ffmpeg.exe 9.0 essentials, `ffmpeg_path` в конфиге), токен агента выпущен (копия на ВМ `~/agent-token-jalal-abad.txt`). Мастер-папка `../ves-agent-jalalabad/` (вне git, СЕКРЕТЫ): сборка + config.toml + com0com + nssm + ffmpeg + `УСТАНОВКА-ДЖАЛАЛ-АБАД.md`
- [x] **Удаление ненужных релизов с экрана «Релизы агентов»** (21.08.2026, запрос Игоря — список версий разросся до 26 строк / 674 МБ на ВМ; места на диске полно (7 из 487 ГБ), чистка ради порядка): кнопка «Удалить» у версий вне каналов («архив»/«не назначен», включая строки-фантомы без файла), stable/pilot удалить нельзя даже прямым POST — сперва «Отозвать»/перевод другой версии. Удаляются разом zip с диска и строка `agent_releases` (`rollout.delete_release`; файл стирается до commit — при отказе диска строка уцелеет); журнал `agent_updates` не трогается — это история обновлений агентов. В confirm — предупреждение, если на версии сидят агенты (работе не мешает, но откат на неё станет невозможен; вернуть версию — только пересборкой из git-тега). Автоочистку сознательно не делали — тихо удаляющая файлы автоматика опаснее пользы. По ревью заодно закрыт старый тупик: строке с каналом, но без файла теперь доступен «Отозвать». Выкачено и применено в тот же день: Игорь оставил 6 версий (0.4.24 stable + 0.4.19–0.4.23 архив «на откат»), каталог 674 → 157 МБ; согласованность БД/диска и целость журнала (18 строк) проверены на бою. 1862 теста. См. decisions 21.08.
- [x] **События weighing.completed — только по весам с привязкой к АИС** (20.08.2026 поздний вечер, вопрос Игоря; ВЫКАЧЕНО на бой): до фикса события непривязанных весов уехали бы в очередь АИС ключом `weighing.completed.unbound` (binding `#` матчит всё). Фильтр двумя слоями: outbox ставится только при `scales.ais_object`; застрявшие события непривязанных весов публикатор закрывает «без отправки» с пометкой (карточка записи показывает «закрыто без отправки: …», не «отправлено»); кнопка «Переотправить» отказывает с note. В контракт v2 — строка-гарантия (7.2) + пометка в истории версии (интерфейс АИС не менялся). **Задел на таможенную интеграцию** (сказал Игорь): своя очередь в том же RabbitMQ, ВСЕ операции (онлайн+офлайн) ВСЕХ объектов — отдельным event_type в том же outbox со своим префиксом ключей, АИС-фильтр не мешает; делать по их контракту (см. decisions 20.08).
- [x] **Подпись «модель индикатора/весов» из центра, агент 0.4.24** (20.08.2026, запрос Игоря — на К-К в шапке не было модели весов, а правка требовала AnyDesk): поле в «Настройках» весов (scales.indicator_model, миграция f2a3b4c5d6e7) → снимок настроек → применяется агентом на лету (AgentInfoLike). Пусто = локальный конфиг. После раскатки 0.4.24 заполнить подписи всех объектов в панели (или скриптом). См. decisions 20.08.
- [x] **Драйвер «vesar» — ВТОРОЙ ДРАЙВЕР ИНДИКАТОРА, агент 0.4.23** (20.08.2026, к установке на Кара-Суу): `agent/drivers/vesar.py` (модуль-близнец cas22 — каркас не выносим до третьего драйвера), реестр `agent/drivers/DRIVERS` + `create_driver`, конфиг `scale.driver = "vesar"`, протокол `SerialScaleDriver` вместо жёсткой привязки к Cas22Driver; проверка XOR-КС, программная стабильность (окно 1 с / ампл. 30 кг, время в тестах подаётся руками), квантование к табло усечением вниз (85,0 → 80; подтвердить сверкой). Эмулятор `tools/vesar_emulator.py` (сценарии как у cas22, `badsum` дополнительно; эталонные пакеты живого дампа воспроизводятся байт в байт — закреплено тестом), спека `docs/protocols/vesar.md`, тесты `tests/test_vesar_driver.py`. См. decisions 20.08.
- [x] **СВХ «Кара-Суу» — ЧЕТВЁРТЫЙ ОБЪЕКТ УСТАНОВЛЕН И ОНЛАЙН** (20.08.2026 вечер, ставил Игорь по УСТАНОВКА-КАРАСУУ.md): объект id=5, весы id=4 «Весы SCS-80» (driver **vesar** — первый не-CAS), legacy .183:8087/1, АИС 0011/№1, агент `karasuu-1` (pilot; 0.4.23 при установке → 0.4.24 раскаткой в тот же вечер, самопроверка пройдена), порт `socket://127.0.0.1:4001` (SERIALTCPIP1 возвращён в Server/127.0.0.1). Камеры со снапшотом 101 и превью 102; роли подтверждены по заезду и поменяны в центре: front=.202, rear=.201 (свап скриптом refs_admin + пересохранение из панели для доставки — прямой скрипт в БД push по WS не делает, доставляет только панель/hello). Мастер-папка `../ves-agent-karasuu/` + zip согласованы. Осталось: поверка и учётка весовщика (за Игорем), шильдик индикатора, NTP камер, сверка веса с UniServer (заодно подтвердит правило усечения 85,0→80).
- [x] **СВХ «Кара-Суу» — обследование** (20.08.2026, завершено установкой в тот же день): выгрузка UniServer разобрана (`../docs/справка/выгрузка-карасуу/`, карточка `docs/objects/kara-suu.md`), код АИС **0011**, весовой ПК 192.168.156.183:8087 (autoscale 1), маршрут открыт. **ПЕРВЫЙ ОБЪЕКТ НЕ НА CAS: протокол «VESAR»** — 12-байтовый пакет STX/ETX, масса ASCII в байтах 1–7, без CRC, стабильность вычисляется программно (1 с / 3 дискреты), поток без запросов; COM4 9600 8-N-1, дискретность 10 кг, НмПВ 500 кг (!). Нужен новый драйвер агента — ждём живой дамп потока (SERIALTCPIP1 на ПК уже включён, параметры уточнить по рецепту Аламедина) и шильдик индикатора. Камеры не Hikvision (XM-формат RTSP `channel=1&stream=0.sdp`, .201/.202) — вероятен путь Д-А (ffmpeg, потоки), проверить HTTP-снапшот. Самый нагруженный объект: ~49 документов/день по нумерации журнала.
- [x] **Журнал объекта у оператора: колонка «Дата и время» вместо «Время», агент 0.4.22** (20.08.2026, запрос Игоря): полные ДД.ММ.ГГГГ ЧЧ:ММ:СС местного времени весового ПК — формат как в панели центра и UniServer; та же дата в подписи лайтбокса фото. Только веб агента, центр не менялся (выкат не требовался).
- [x] **Частое превью оператора лёгким кадром (preview_url), агент 0.4.21** (20.08.2026, запрос Игоря по Аламедину — 6 МП камеры медленно отдают полный кадр, превью дёргалось): у камеры в «Справочниках» третье необязательное поле «Превью (HTTP)» — снапшот суб-потока (`channels/102/picture`, на Аламедине проверен); задан → превью на агенте раз в 1 с по лёгкому URL, пусто → по-старому раз в 2 с; фото операций всегда с основного URL (правило №2). Отклонено пережатие на агенте (узкое место — съёмка, не отдача; см. decisions 20.08). Центр: миграция `e1f2a3b4c5d6` (cameras.preview_url), справочник с валидацией (превью только вместе с основным URL), снимок настроек несёт поле агенту на лету. Раскатано Игорем в тот же день: все три агента на 0.4.21 (сверено с боевой БД), превью-URL заполнены обеим камерам Аламедина, частое превью работает; на К-К и Д-А поле пусто — прежнее поведение.
- [x] **СВХ «Аламедин» — ТРЕТИЙ ОБЪЕКТ, агент установлен и онлайн** (19.08.2026): выгрузка UniServer (`../docs/справка/выгрузка-аламедин/`, карточка объекта `docs/objects/alamedin.md`) — индикатор **тот же CAS 22 byte**, конфиг WeightIndicator1 байт в байт как на К-К (COM4 на PCI-E карте WCH, 10 кг, НмПВ 200), камеры Hikvision 6 МП (3200×1800) — HTTP-снапшот работает, ffmpeg не нужен; табло/RFID/шлагбаумы в UniServer не подключены. В центре заведены объект `alamedin`, весы id=3 «Весы Соболь-80» (legacy-маршрут 192.168.159.251:8087/autoscale 1, привязка АИС v2 `0001`/№1), камеры (front=.242 видит кабину, rear=.241 — подтвердить по снимкам), настройки цикла 200/500, агент `alamedin-1` (токен на ВМ `~/agent-token-alamedin.txt`). Мастер-папка `../ves-agent-alamedin/` (вне git, СЕКРЕТЫ), агент в `D:\vesy-agent`. **Урок: на Windows 11 этого ПК драйвер com0com не грузится** (`sc start com0com` → 577, подпись) — вместо пары портов UniServer ретранслирует поток индикатора в TCP (модуль SERIALTCPIP1: Mode=Server, Host=127.0.0.1, Port=4001, SourceRepeaterSerialPort=SerialPort_WI1), агент читает `port = "socket://127.0.0.1:4001"`; для этого выпущен **агент 0.4.20** (в сборку PyInstaller добавлены обработчики URL pyserial — 0.4.19 падал «protocol 'socket' not known»). 0.4.20 раскатан каналами на все три объекта («В pilot» → «В stable»), у всех «самопроверка пройдена» — первый боевой прогон самопроверки/автоотката 0.4.19. Второй урок: кнопка «Перезапуск сервера» в вебе UniServer уронила его насовсем — поднят `sc start UniServerAuto`; перезапускать UniServer только службой. Вечером 19.08 Игорь закрыл хвосты: роли камер подтверждены по снимкам и поменяны местами в панели (front=.241, rear=.242 — догадка по кадрам UniServer снова оказалась обратной), свидетельство о поверке №4906 (03.03.2026 — 03.03.2027; в документах до 04.09.2026 по ошибке значилось №5535 — исправлено по базе и фото свидетельства) и учётка весовщика `alamedweight` заведены. Осталось по желанию: `AutoScale1.glob_AutoFixingMode=0` в UniServer, NTP на камерах, шильдик индикатора; мёртвый com0com не трогаем — при остановке UniServer агент переводится на физический COM4 из панели.
- [x] **ПЗТК «Джалал-Абад» — установка агента** (12.08.2026, ставил Игорь по чек-листу без AnyDesk-марафона): маршрут 192.168.154.x → 140.70 прописал системщик; на весовом ПК доступа к C: нет — агент живёт в **D:\vesy-agent** (bat-скрипты и самообновление работают от своей папки, менялись только пути в config.toml); фактическая пара com0com **COM14↔COM15** (UniServer ретранслирует COM3→COM14, агент на COM15; настройки весов в центре приведены). Агент 0.4.5 подключился к центру, вес пошёл. Камеры сперва падали по таймауту 5 с → выяснено (сверка съёмкой с мака: обе камеры отдают кадр за 1,4–2,3 с) — медленный запуск ffmpeg с HDD + ключевой кадр RTSP; решение — релиз 0.4.6
- [x] **ПЗТК «Джалал-Абад» — агент обновлён до 0.4.6 кнопкой панели** (12.08.2026): обновлению 6 раз подряд мешал антивирус **360 Total Security** (Permission denied на записи ucrtbase.dll в app_new — блокирует запись системных DLL неподписанным процессом; Defender на ПК выключен самим 360, GUI «Безопасность Windows» пустой). Диагностика по отчётам update_status в логах центра, без выезда. Вылечено добавлением в белый список 360 **файла** `D:\vesy-agent\app\ves-agent.exe` (белый список папки не действует — 360 решает по процессу-писателю); карантин 360 при этом пуст. После обновления агент на 0.4.6, инструкции дополнены разделом про антивирус, в этап 2 добавлены distr-задачи (распаковка bat'ом, подпись сборок)

## Этап 3 — динамические весы 🔵 (обследование Торугарта начато 25.08.2026)

- [x] **Обследование «Торугарт» по документации** (25.08.2026; сканы от Игоря `../docs/Торугарт/`, выжимка — `docs/objects/torugart.md`): НЕ XK3190-DM, как значилось по реестру, а **двое Тензо-М ВА-Д-20** (зав. 74688/74689, 2019 г.) — ПООСНЫЕ весы (платформа 3×6 м, статика с наездами + движение ≤8 км/ч, полная масса = сумма осей, d=10 кг, ГОСТ 33242 классы B/1) + СОДИ ТС (светофоры/шлагбаумы/видеофиксаторы/RFID/табло/лучевые датчики габаритов). У каждых весов свой ПК → два агента. ГЛАВНОЕ: готового веса в потоке НЕТ — ПД-304Н шлёт сырые коды АЦП (RS-422→АИП-021→RS-232), всю обработку динамики делает ПО «Статика 3.0» (Тензо-М) на ПК; протокол не опубликован. Предварительный вывод: интегрироваться поверх штатного ПО (экспорт результатов), НЕ реверсить сырой поток (метрологически значимая обработка). Акт для динамики будет другой — поосный (образец действующего у Игоря). Таможенная интеграция: все операции всех объектов + название объекта/весов в событии (уточнение к decisions 20.08); габаритные датчики — модели не определены, в модель заложить расширяемость. ОТКРЫТЫЕ ВОПРОСЫ заданы Игорю 25.08 — список в torugart.md (какое ПО фактически, статус монтажа, запрос протокола/экспорта у Тензо-М, основной режим, кто считает превышения, контракт таможни, будет ли АИС, чьи габаритные датчики)
- [ ] Изучить Уралвес ВАТ-Д-20 (Кен-Сай), снять выгрузки/логи
- [ ] По ответам на вопросы Торугарта: спроектировать интеграцию (поверх «Статики 3.0» или иначе), модель данных поосного взвешивания (оси/расстояния/скатность/подвеска/скорость/режим), поосный печатный акт с нормативами и превышениями, драйвер/коннектор, экранные формы

## Этап 4 — аналитика 🟡 (начат 18.08.2026 по решению Игоря — раньше остатка этапа 2)

- [x] **Экран «Отчёты» (`/panel/reports`, макет 5.7 center-reports + расширения)** (18.08.2026, см. decisions): модуль `center/reports.py` — только чтение журнала и служебных таблиц, период по бишкекским суткам (пресеты сегодня/вчера/7 дней/30 дней/этот месяц/прошлый месяц/квартал/год или произвольные даты; будущее и >3 лет обрезаются), объект по фильтру и PanelScope (диспетчер объекта видит только свой — и в выгрузках), момент операции `coalesce(weighed_at, created_at)` как в журнале, только состоявшиеся операции, сторно парой. Блоки: ИТОГИ (взвешиваний, тарирований, офлайн-операций с долей, суммарное нетто т, без нетто, отказов команд АИС из audit_log, офлайн без номера АИС, средний брутто — у четырёх главных пилюля Δ и значение прошлого периода); МАССЫ (три числа: нетто по взвешиваниям с тарой = Σбрутто − Σтары, «без тары: N, Σбрутто», итого брутто; причины «без нетто» той же логикой, что примечание карты — не было / устарело / номер не передан / была, но не подставилась; таблица «сцепки к перетарированию» со ссылкой на историю тарирований); ПО ОБЪЕКТАМ (таблица: взвешиваний, тарирований, офлайн с долей, нетто т, средний брутто, доступность, инцидентов, отказов АИС + два горизонтальных SVG-графика — взвешиваний и нетто); ДИНАМИКА (шаг по длине периода — дни/недели/месяцы, столбики взвешиваний и нетто с пустыми отрезками, переключатель «по объектам» — линии на общей шкале + таблица отрезков, по умолчанию свёрнутая — просьба Игоря 18.08, в печати раскрыта); РУЧНЫЕ (ОФЛАЙН) ОПЕРАЦИИ по объектам и операторам (доля от операций объекта, без номера АИС, из них старше 3 суток); НАДЁЖНОСТЬ (доступность по переходам детектора «офлайн» с состоянием на начало периода и обрезкой «сейчас», уходов в офлайн, суммарный офлайн, инциденты индикатора/камер/прочие, отказы АИС по кодам — v2 по scale_id, v1 по legacy-адресу); СРАВНЕНИЕ с прошлым периодом (календарный пресет — те же числа единицей раньше; отрезок с 1-го числа — месяцем раньше; прочее — смежный той же длины). Печатная версия `/panel/reports/print` (без шапки, print-CSS A4 landscape), выгрузки `/panel/reports/export.csv` (один файл, блоки подряд, формат как у журнала) и `/panel/reports/export.xlsx` (openpyxl: листы Итоги / По объектам / Динамика / Ручные операции / Надёжность / Сцепки к перетарированию, числовые форматы, закреплённая шапка). Графики — `center/web/charts.py`, серверный SVG в палитре ДС без внешних JS. Вкладка «Отчёты» в шапке для всех ролей. Демо-посев: `tools/seed_demo_center.py --history 90` досеивает историю в demo-БД (будни/выходные, устаревшие тары, операторы, номера АИС, события мониторинга, отказы АИС). Миграция d0e1f2a3b4c5 — индекс `audit_log (action, at)` (отказы АИС читаются из журнала аудита). 47 новых тестов, всего 1650 (test_center_reports — агрегаты на живом PostgreSQL с посевом под каждое правило, включая границу правила №4 в SQL; test_center_reports_panel — маршруты, область видимости, CSV/xlsx с защитой от формул, разбор параметров с полом дат, графики). Правки по ревью: причины «без нетто» агрегатом в SQL, отказы АИС фильтром в SQL и один раз на отчёт, xlsx-текст строкой, пол дат 2000 г., посев истории берёт тару «на тот момент» по журналу. Проверено глазами в dev-панели на 90-дневной истории (дни/недели/месяцы, линии по объектам, печать). Сознательно не делается: импорт истории UniServer, PDF, рассылка (согласовано с Игорем)
- [x] Объёмы на дашборде (18.08.2026, по «Давай сделаем» Игоря): полоска из трёх карточек-ссылок «Сегодня / 7 дней / Этот месяц» (взвешиваний, нетто т, тарирований, офлайн; у недели и месяца — Δ к прошлой неделе / тем же числам прошлого месяца) + карточка «Отчёты → Подробно»; живёт в HTMX-фрагменте дашборда (обновляется с счётчиками), считается ОДНИМ запросом по журналу с FILTER по пяти окнам (`reports.volume_summary`), под PanelScope; клик по карточке открывает отчёт за тот же период. 4 новых теста (1655)
- [ ] Интеграции помимо АИС — при необходимости (запроса нет)
- [ ] Возможные расширения «Отчётов» — только по запросу Игоря после боевого использования: сравнение год-к-году (появится само с накоплением истории — пресет «Этот год» уже есть), выгрузка «Итоги» одной строкой в месяц для бухгалтерии, отчёт по конкретной сцепке/перевозчику (сейчас — история тарирований + фильтр журнала по номеру ТС)

---

## Вопросы к Игорю

- [x] **Релиз 0.4.28 переведён в stable** (Игорь, вечер 03.09.2026; Д-А догнал сразу — все шесть агентов на 0.4.28, самопроверки пройдены)
- [ ] **Докачка архива обновления с места обрыва (HTTP Range) — ОТЛОЖЕНО решением Игоря 03.09.2026 («пока делать не будем»)** (03.09.2026: на Кара-Суу загрузка 27 МБ ползла ~25 КБ/с и оборвалась на 16,5 МБ, агент отверг по размеру, вторая автоматическая попытка через 40 мин прошла; на медленном канале каждая попытка качает всё заново). Не срочно — делать, если обрывы повторятся; заодно спросить системщика про канал Кара-Суу
- [ ] **Очередь `ais-svh.weighings` (vhost `vesy`) никто не читает — 120 сообщений, 0 консьюмеров, НО операции в АИС ЕСТЬ** (04.09.2026; сначала было принято за срочное — ошибка: по аудиту АИС забирает офлайн-операции НЕ из очереди, а сверкой по REST — 04.09 в 14:50 за 14 секунд 120 `ais_ref_link` (`PUT …/ais_ref`) по всем 120 ручным операциям Кокчо-Коза 03–04.09 + 374 `photo_download`; номера привязаны: `WEI000100169`…, `TAR000012945`…). Очередь при этом копится (durable, без TTL, ~120 сообщений в сутки) — спросить программистов АИС, нужен ли им RabbitMQ вообще: если сверяются по REST, договориться вычитывать события или отключить публикацию; пока не трогать. Урок: «0 консьюмеров» ≠ «операций в АИС нет» — сначала смотреть аудит `ais_ref_link`. Проверка очереди: `cd ~/vesy-gti/deploy && docker compose exec -T rabbitmq rabbitmqctl list_queues -p vesy name messages consumers`
- [x] **Защита от гружёных «тарирований» и сторнирование — СДЕЛАНО** (04.09.2026, вечер, по решению Игоря: лимит 25 т, причина сторно обязательна): повод — 04.09 с 11:47 по 19:30 через АИС на Кокчо-Козе проведено 23 тарирования массой 31–43 т (ids 220–262, все с номерами TAR АИС; настоящие тары объекта 13,6–17,6 т). (1) Сторно из панели (центр): страница записи → «Сторнировать» (админ, причина обязательна) → запись-сторно `storno_of` + аудит `weighing_storno`, строка реестра тары сцепки пересобирается по журналу и снимок разлетается агентам (`hub.broadcast_tare_registry`), `latest_taring_as_of`/реестр аннулированные не видят, счётчик дашборда и отчёты пару не считают, пилюли «Сторно»/«Аннулирована», CSV помечает пару; API v2: записи-сторно скрыты из GET-списка/по id/из `PUT …/ais_ref` (сверка АИС приняла бы их за новые операции), у исходной записи поле `storno`, у вложения `tare` — `storno` (тара была подставлена, потом аннулирована). (2) Агент 0.4.29: `CycleSettings.max_tare_kg` (умолчание 25 000, 0 — выкл; «Настройки» весов → «Лимит массы тары, кг»; `[cycle] max_tare_kg` локально) — тарирование тяжелее лимита отклоняется ДО снимков и записи в обоих режимах: АИС получает `200` + `ERR_TARE_TOO_HEAVY` с массой и лимитом (контракт 4.4, одностороннее дополнение), оператор — сообщение на форме; тара ≥ брутто при взвешивании не подставляется (нетто пусто, причина в `message` записи, карта печатает примечание, документ `tare.reason: tare_not_below_gross`/`replica_lag`). (3) Центр: события мониторинга `tare_rejected` (отказ по лимиту, с номером TAR и ТС) и `tare_implausible` (тара ≥ брутто не подставлена либо старый агент записал нетто ≤ 0) — «События» + Telegram. Ревью и QA перед коммитом: фолбэк API v1 больше не подставляет тару ≥ брутто (иначе АИС получала бы отрицательное нетто), отчёт «причины без нетто» не видит аннулированные тарирования и записи-сторно, событие `tare_implausible` считается по журналу на момент взвешивания (без аннулированных, с проверкой срока), одно сторно на запись закреплено частичным уникальным индексом (миграция c5d6e7f8a9b0, гонка → StornoError), печатная карточка аннулированной записи несёт пометку «АННУЛИРОВАНА сторно от …», в агенте — фиксированное смещение Бишкека вместо zoneinfo (tzdata в замороженной сборке может не быть), код `ERR_TARE_TOO_HEAVY` дописан и в контракт v1. Тесты: +93 (сторно queries/routes/API/реестр/индекс, лимит и правдоподобие в auto/manual/card/settings/v1/отчётах), всего 2050. ОСТАЛОСЬ ИГОРЮ: сторнировать 23 записи Кокчо-Коза из панели (ids 220–262; список с TAR — файл в переписке 04.09) и передать список АИС для аннулирования их документов; нажать «В pilot» для 0.4.29, затем stable.
- [x] **Предупредить программистов АИС «СВХ» про Кокчо-Коз (0021)** (03.09.2026 → закрыто 04.09.2026: АИС подключена, галочка снята; осталось вычитать очередь — пункт выше): пока АИС к объекту не подключена, на весах включён «Ручной режим при связи» — в их очередь `ais-svh.weighings` идут события `weighing.completed` ручных операций (`source: local_offline`, `ais_ref: null`) — импортировать или отбрасывать осознанно (контракт v2, уточнение 03.09.2026 в строке версии). После подключения АИС — снять галочку в «Настройках» весов (аудит `scale_manual_mode`, бейдж на карточке напомнит)
- [ ] **Jevol JLS-150 (габариты для Торугарта) — ждём ответы поставщика на второй раунд** (03.09.2026; Игорь пишет напрямую; письмо EN на 27 вопросов + ключ оценки — `docs/objects/torugart-jevol-questions.md`): блокеры (1) документированный интерфейс данных + образец записи + демо, (2) исполнение до −40 / модель лидара; плюс ANPR (потеря измерений без номера, шаблоны КР/КЗ/КНР), автопоезд как одно ТС, длина > 20 м. По ответам — заключение «устраивает / не устраивает, потому что …» по таблице в файле. Параллельно — цена лазерного сканера габаритов у Тензо-М
- [x] **СВХ «Кокчо-Коз» — ПОДКЛЮЧЁН 03.09.2026 за один день** (объект id=7, весы id=6 «Весы XK3190-DS3» driver xk3190, АИС 0021/№1, поверка №857, камеры Dahua .123 front/.122 rear подтверждены по первому запуску, агент kokcho-koz-1 pilot онлайн; штатное ПО «ВесыСофт»+«Server Auto» ВЫКЛЮЧЕНО решением Игоря — агент прямо на COM1/1200, ретрансляция не понадобилась; кадр DS3 = VESAR в ДЕСЯТЫХ кг (первый запуск 800 вместо 80 → `weight_divisor = 10` в конфиге, умолчание драйвера исправлено в 0.4.28); программистам АИС переданы адрес центра + 0021/№1 — АИС к объекту ещё не подключена). Ручной режим при связи ВКЛЮЧЁН и ПРОВЕРЕН 03.09 вечером (ручное взвешивание прошло; для проверки человеком пороги временно 20/60, возвращены на 400/800). ОСТАЛОСЬ: (1) АИС v2 для 0021 — за программистами АИС; до этого галочка «Ручной режим при связи» (0.4.28) остаётся — ВЫКЛЮЧИТЬ после подключения АИС и предупредить АИС, что ручные операции приедут им событиями из очереди; (2) учётка весовщика (Игорь); (3) шильдик индикатора, NTP камер, дамп потока для спеки — при случае
- [ ] **Ежедневный бэкап БД на ВМ — ОТЛОЖЕН решением Игоря 02.09.2026** («пока не запустились полноценно, данные мусорные; поставим при полноценном запуске» — вернуться к пункту при переключении АИС). Исходно: (найдено 02.09.2026 при чистке: deploy/README §7 описывает cron → /backup, на ВМ нет ни каталога, ни задания; единственные копии — ручные `~/backups/*-before-cleanup-2026-09-02.*`). Готовая строка crontab (ежедневный `pg_dump -Fc` + ротация 30 дней) — в README; поставить по команде Игоря «ставь»
- [ ] **Локальные журналы агентов после чистки центра** (02.09.2026; следствие уже видно: после «Освободить» на К-К записи от 21.08 в журнале агента — с прочерками вместо фото, их копий в центре нет; чистка локальных журналов убрала бы и эти хвосты): на ПК всех пяти объектов в SQLite агента остались тестовые записи — оператор видит их в своём журнале, на работу и нетто не влияет (реестр тар пришёл из центра пустым). Чистить только руками через AnyDesk: остановить службу ves-agent → удалить файл БД агента (db_path из config.toml) и локальные фото (photos_dir) → запустить службу; реестры тар/операторов агент получит заново при hello. Нужно ли — решает Игорь
- [ ] **ПЗТК «Беловодское» — по готовности нового ПК** (оценка 27.08.2026, карточка belovodskoe.md): код АИС и № весов; есть ли камеры; кто заказчик интеграции (весы принадлежат Филиалу ЗАО «UBS Transit»); свежая поверка (№6673 истекает 19.09.2026); при визите — дамп потока XK3190-D2+ напрямую с COM (UniServer-а нет) → драйвер xk3190
- [ ] **СВХ «Алтын логистик» — финальные мелочи** (26.08.2026 поздний вечер: роли камер ПОДТВЕРЖДЕНЫ по заезду — front=.20/rear=.21, догадка ВПЕРВЫЕ верна после 3 обратных; сверка с UniServer на первых машинах СХОДИТСЯ; учётка весовщика создана; весы переименованы Игорем в «Весы Пегас 22-80-А-10»): (1) пробное взвешивание имитатором АИС v2 — подготовлено, машина уехала до команды; рецепт запуска изнутри контейнера app (токен из env, ais_ref WEI000990001) — в карточке; сделать на любой удобной машине; (2) d/e с шильдика весов и имя ПК/антивирус/переходник COM3 — при случае, не блокер.
- [ ] **ВРЕМЕННО открыта панель боевого RabbitMQ (20.08.2026, просьба разработчиков АИС «посмотреть и потыкать»; их консьюмер уже вычитал 4 события из ais-svh.weighings). С 21.08.2026 порт `15672:15672` — В COMPOSE РЕПОЗИТОРИЯ (решение Игоря: за сутки ship затирал ручное открытие трижды, а классификатор блокирует sed compose на ВМ) — ship больше НЕ закрывает, переоткрывать после выкатов НЕ нужно**: порт 15672 наружу + учётке `ais-svh` тег `management` (в панели видит только vhost vesy и свою очередь; права не расширялись — по-прежнему только чтение `ais-svh.weighings`). Адрес для разработчиков: `http://192.168.140.70:15672`, логин `ais-svh`, пароль на ВМ `~/rabbit-ais-svh.txt` (передаёт Игорь). **ЗАКРЫТЬ, когда посмотрят**: в deploy/docker-compose.yml репозитория вернуть `127.0.0.1:15672:15672` (комментарий-инструкция прямо там) → коммит → ship (или sed на ВМ + `docker compose up -d rabbitmq`); тег снять `rabbitmqctl set_user_tags ais-svh` (пустой). Полного administrator сознательно НЕ давали (мог бы удалять очереди/сообщения, менять пароли, рвать соединения центра).
- [x] Фото на взвешивании: 2 или 4? — ЗАКРЫТО конфигом 07.08.2026: реально настроены только 2 камеры (Camera3/4 без URL) → снимаем 2 фото
- [x] Срок актуальности тары — ЗАКРЫТО 07.08.2026: 3 месяца, как в АИС (конфигурируемо). Справочник тары не используем; офлайн-оператор вводит только номера АТС/прицепа. Время — везде Бишкек, в контрактах с поясом +06:00 (тоже решено 07.08.2026)
- [x] Выгрузка с Кызыл-Кыи по чек-листу — ✅ ВЫПОЛНЕНА 07.08.2026 (конфиг, CSV журнала, фото, версии, камеры — см. docs/uniserver-kyzylkia.md; материалы в ../docs/справка/выгрузка-кызылкыя/, там пароли — в git не копировать)
- [ ] Поручить настроить время/NTP на камерах Кызыл-Кыи (в кадре штамп 1970 от OSD самой камеры — подтверждено и пробным взвешиванием 10.08.2026; наш оверлей с верной датой прожигается, но чужой штамп в кадре мозолит глаза). На камерах Джалал-Абада ТО ЖЕ САМОЕ (1970 в кадре, скриншоты 12.08.2026) — поручить вместе
- [ ] **Сверка Джалал-Абада: расхождение с UniServer 0/+10 кг — систематика, наш агент прав** (найдено Игорем 12.08.2026, три случая: 43850/43840, 43770/43760, 42970/42960). Индикатор шлёт вес шагом 10 кг (протокол CAS = трансляция табло), UniServer огрубляет ВНИЗ до своей настроенной дискретности 20 (`Massa_Discret=20.000` в выгрузке). Критерий сверки для этого объекта: Δ ∈ {0, +10} кг = совпадение; на Кызыл-Кые — строгое совпадение (дискретности сходятся: обе 10). ПРИ СЛУЧАЕ на объекте: подтвердить по табло индикатора и глянуть паспорт поверки — если по паспорту d=20, добавить агенту квантование по дискретности весов (параметр в центре)
- [x] Маршрут сети для Джалал-Абада — ✅ ЗАКРЫТО 12.08.2026: системщик прописал маршрут 192.168.154.x → 192.168.140.70, агент установлен и работает; боевой оператор объекта заведён Игорем в панели
- [x] Создать репозиторий на GitHub — `git@github.com:Rainthium/vesy-gti.git` (07.08.2026)
- [x] **Telegram-бот для алертов мониторинга** — ЗАКРЫТО 13.08.2026 вечером: Игорь завёл бота @vesy_gti_alerts_bot и группу «Алерты весов ОАО "ГТИ"», токен и chat_id (-5485263200) вписаны в deploy/.env на ВМ (правило №7 — в git их нет), центр выкачен с ними, тестовое сообщение в группу дошло. Нюанс на будущее: getUpdates была пустой, пока в группе не написали сообщение с УПОМИНАНИЕМ бота — privacy mode не показывает боту обычные сообщения групп. (История: 14.08 группа мигрировала в супергруппу и сменила chat_id — алерты переведены в КАНАЛ, группа удалена; актуальное состояние — в пункте мониторинга этапа 2)
- [x] Доступ к RabbitMQ — СНЯТО 17.08.2026: брокер ставим свой на ВМ центра (docker-compose), чужой доступ не нужен; от системщика — открыть 5672 с сервера АИС до 192.168.140.70
- [x] Контракт с разработчиками АИС: поля `operation`, `vehicle_number`/`trailer_number` в запросе, tare/netto/no_valid_tare в ответе v1, парсинг дат с поясом +06:00 — ПОДТВЕРЖДЕНО Игорем 08.08.2026
- [ ] Модели индикаторов и выгрузки настроек с остальных объектов (этап 0 обследования)
- [x] Адрес/доступ ВМ Ubuntu для развёртывания центра — ЗАКРЫТО 10.08.2026: ВМ 192.168.140.70 (Ubuntu 22.04, 4 vCPU/8 ГБ/487 ГБ), центр развёрнут и работает; SSH-ключ настроен; домен vesy.gti.kg (публичный, через корпоративный прокси) — для панели, агенты ходят напрямую по IP
- [x] Подстановка тары по ПРИЦЕПУ — РЕШЕНО Игорем 09.08.2026 («делай по паре»): тара привязана к сцепке голова+прицеп, подставляется только при совпадении обоих номеров; тарирование без прицепа действует только для машины без прицепа. ВНЕДРЕНО 09.08.2026 (см. задачу в этапе 1 и decisions)

## Журнал вех

- 06.08.2026 — прямое чтение веса с индикатора CAS CI-201A подтверждено на СВХ «Кызыл-Кыя» (прототип cas22_reader.py, фиксация 1460 кг)
- 06.08.2026 — архитектура v1.1 согласована; дизайн 14 экранов принят
- 07.08.2026 — решения: хранение фото 5 лет; GitHub; код англ./комменты рус.; разработка на эмуляторе
- 07.08.2026 — выгрузка с Кызыл-Кыи выполнена (конфиг UniServer, журнал, камеры, версии → docs/uniserver-kyzylkia.md); решения: АИС не трогаем (имитатор), тара только из тарирований, 2 фото + свой оверлей, время с поясом +06:00
- 10.08.2026 — центр развёрнут на боевой ВМ 192.168.140.70; агент Кызыл-Кыи в бою параллельно UniServer (первая боевая запись 27 080 кг с фото)
- 12.08.2026 — второй объект пилота: ПЗТК «Джалал-Абад» подключён (RTSP-потоки 0.4.7, антивирус 360 приручён белым списком exe); найдена систематика сверки Δ∈{0,+10} — прав наш агент
- 13.08.2026 — печатная весовая карточка подтверждена на бумаге (Canon MF3010, 100%); мониторинг с Telegram-алертами сделан и выкачен за вечер
- 14.08.2026 — пожелания Игоря за день: история тарирований, учётки агентов в центре + кнопка блокировки местных, камеры К-К исправлены из панели; три релиза агента кнопками (0.4.13→0.4.14→0.4.15), быстрая распаковка подтверждена при включённом 360
- 14.08.2026 (вечер) — показ устаревшего тарирования при взвешивании (карта/запись/подсказки оператора, дата и масса; расчёт и контракт v1 не тронуты), агент 0.4.16 установлен кнопками — четвёртый релиз за день
- 17.08.2026 — контракт v2 с АИС «СВХ» согласован за день (0.1 → 0.2 → 0.3 → 1.0) и реализован целиком (API v2, outbox + RabbitMQ на ВМ, панель, инструменты); выкачено ночью
- 18.08.2026 — агенты 0.4.17/0.4.18 (номер АИС в записи, доклад сторожка, сессии следуют за паролем) кнопками; доступы АИС переданы; АВТОВЫКАТ ПО КАНАЛАМ + самопроверка/автооткат + экран «Релизы» сделаны и выкачены, первый боевой автовыкат 0.4.19: К-К через pilot за 14 с, Д-А через stable за 15 с — кнопки карточек больше не нужны
- 18.08.2026 (вечер) — начат этап 4: экран «Отчёты» (итоги, массы, по объектам, динамика, ручные операции, надёжность, Δ к прошлому периоду, печать, CSV/.xlsx) сделан целиком и выкачен на ВМ; по просьбам Игоря — таблица под графиками свёрнута, на дашборде полоска объёмов «Сегодня / 7 дней / Этот месяц» с Δ
- 27.08.2026 — заочная оценка шестого кандидата ПЗТК «Беловодское» (XK3190-D2+, без UniServer): интегрируем без замены индикатора, ждём новый ПК; спека ПК отправлена объекту
- 02.09.2026 — боевой журнал очищен от тестовых операций по решению Игоря (81 запись, 150 фото, 3 тары; бэкап на ВМ; триггеры неизменяемости на месте) — отсчёт боевых данных заново
- 02.09.2026 — заочное обследование СВХ «Кокчо-Коз» (XK3190-DS3, ВесыСофт вместо UniServer): план ретрансляции com0com+hub4com, скрипт дампа COM без UniServer; ждём сеть
- 02.09.2026 — агент 0.4.25: срок хранения локальных фото из центра и кнопка «Освободить место» (вместо удаления записей за период — правило №2), «Диск ПК» на карточке весов; 1910 тестов; раскатан Игорем на все объекты в тот же вечер
- 02.09.2026 (вечер) — агент 0.4.26: журнал после уборки фото помнит отказ центра, миниатюры из центра — на диск; вёрстка карточки весов; 1915 тестов
